# Standalone lint pass on generated outputs.
#
# Runs on every PR touching outputs/tests/** to keep the generated suite
# clean over time. eslint-plugin-playwright autofix handles ~50% of test
# smells for free per research (arxiv:2410.10628 — post-processing > prompt
# engineering for test smells).
#
# This is intentionally separate from migrate.yml so:
#   - Pure cosmetic fixes (linter rule bump, prettier config change) don't
#     need to re-trigger an LLM run.
#   - Humans editing generated tests after merge get the same lint gate.

name: Lint generated tests

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'outputs/tests/**'
      - '.eslintrc*'    # legacy — kept for safety
      - 'eslint.config.js'  # v9 flat config (actual config in use)
      - 'package.json'
  push:
    branches: [main]
    paths:
      - 'outputs/tests/**'
      - 'eslint.config.js'

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install deps
        run: npm ci

      - name: TypeScript strict
        run: npx tsc --noEmit -p outputs/tests/tsconfig.json

      - name: ESLint (no fix — surface issues)
        run: npx eslint 'outputs/tests/**/*.ts'

      - name: Playwright parse
        shell: bash
        run: |
          set -euo pipefail
          # playwright test --list interprets a bare glob as a test-name regex
          # ('Nothing to repeat' error). Enumerate spec files via find first.
          mapfile -t SPECS < <(find outputs/tests -type f -name '*.spec.ts')
          if [ "${#SPECS[@]}" -gt 0 ]; then
            npx playwright test --list "${SPECS[@]}"
          else
            echo "No .spec.ts files under outputs/tests — nothing to lint-parse."
          fi

      - name: Forbidden patterns hard-fail check
        shell: bash
        run: |
          set -euo pipefail
          FAIL=0
          # Hard-fail patterns from config/migration-rules.md §8.
          for pattern in \
            'waitForTimeout' \
            'force: true' \
            'page\.pause()' \
            '\.nth(' \
            'test\.only' \
            'test\.skip' \
            ': any\b' \
            'as unknown as' \
            'console\.log' \
          ; do
            # Exclude playwright.config.ts — it's runtime config, not test
            # code, and may legitimately reference types/identifiers that
            # look like forbidden patterns (e.g. actionTimeout: 5000).
            if grep -rE "$pattern" outputs/tests/ --include='*.ts' --include='*.tsx' --exclude='playwright.config.ts'; then
              echo "::error::Forbidden pattern '$pattern' found in generated tests"
              FAIL=1
            fi
          done
          if [ "$FAIL" -ne 0 ]; then
            exit 1
          fi
