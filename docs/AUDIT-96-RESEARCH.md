# 96%-readiness audit — research agent results (raw)

> Verbatim output of the multi-agent audit (2026-06-22): 10 discovery dimensions →
> triage/dedup/rank → adversarial verify → solution research. Saved so the work is
> durable. Prioritized roadmap: [AUDIT-96-FINDINGS.md](./AUDIT-96-FINDINGS.md);
> remaining work: [AUDIT-96-NEXT-STEPS.md](./AUDIT-96-NEXT-STEPS.md).

## Part A — triaged top problems (ranked, with merged evidence)

### grounding-cap-permanently-unreachable — sev 5 [trustworthy-measured-quality]
**DOM grounding is structurally dead: the cap can never lift on real pwm-blueprint output, and Stage 1 never captures a snapshot**

- **Problem:** The 'scorer is now honest' claim rests on DOM grounding, but grounding can NEVER be achieved on real pwm-blueprint output and the plan stage never captures a snapshot at all. (a) domProbeConfirmed (evaluate.ts:685-694) requires totalLocators>0, but the probe is fed only the spec file (migrate.yml:1162), and pwm-blueprint specs contain ZERO locators (verified: all 5 specs return 0 getBy*/locator calls; every locator lives in the POMs, cart.page.ts has 5). So domGrounded is permanently false and the UNVERIFIED_LOCATOR_CONFIDENCE_CAP=0.69 (evaluate.ts:444,460) is a permanent floor — nothing can auto-ship even with a live SUT, OR operators conclude grounding is broken and disable the cap, restoring the original anti-correlated auto-ship. (b) Stage 1 (plan.yml) never calls dom-snapshot capture; there is no outputs/dom-snapshots dir and zero plans carry a // dom-snapshot:role= annotation (verified empty). The analyze.md Step 4b grounding contract references a snapshot file that can never exist at plan time. (c) Stage 2 (generate.md) has zero DOM-snapshot references, so even a grounded plan annotation dies at the analyze→generate boundary. The most expensive half of the grounding investment produces no protection at the point the locator is actually written.
- **Evidence:** evaluate.ts:444 (UNVERIFIED_LOCATOR_CONFIDENCE_CAP=0.69), :460 (if !domGrounded return min(raw,CAP)), :685-694 (domProbeConfirmed requires totalLocators>0 && resolvedUnique>0 && notFound===0); migrate.yml:1162 (probe fed spec file only); verified all 5 outputs/tests/*.spec.ts have 0 locator calls while cart.page.ts has 5; plan.yml only references dom-snapshot in 2 read-only spots, no capture step; `ls outputs/dom-snapshots` = no such dir; grep dom-snapshot:role across outputs/plans = 0 files; analyze.md:128-167 grounding contract reads a snapshot that never exists at Stage 1; generate.md has zero dom-snapshot/grounding references; cart.page.ts:21-29 ships getByTestId('cart-subtotal')+getByLabel(/quantity/i) with false 'CSS class locator kept as fallback' comments and no fallback present.
- **Source dimensions:** scorer-trust, dom-grounding-maturity, rag-quality, prompt-kb-coherence, pipeline-engineering
- **Verify verdict:** isReal=True sevConfirmed=5 — Independently confirmed every load-bearing element by reading the actual files (note: real paths are scripts/evaluate.ts and .github/workflows/migrate.yml, not the bare names cited, but the cited line numbers match).\n\n(a) CAP PERMANENTLY UNREACHABLE — CONFIRMED. evaluate.ts:444 UNVERIFIED_LOCATOR_

### ci-scores-wrong-spec — sev 5 [trustworthy-measured-quality]
**CI scores the WRONG spec: `find … | head -1` evaluates the lexically-first prior migration, not the current one**

- **Problem:** The authoritative confidence/verify decision in CI is computed against OUT_SPEC=$(find outputs/tests -maxdepth 2 -name '*.spec.ts' -not -path '*/_legacy-v0.1.x/*' | head -1) (migrate.yml:1220-1231). outputs/tests/ is a flat dir already holding every merged spec, so head -1 always returns force-clicks.spec.ts regardless of which input is being migrated. evaluate.ts therefore scores force-clicks, writes its confidence into the current input's report (outputs/reports/<base>.md), and the confidence<0.7 verify trigger fires on the wrong artifact. The AST-diff-trivial gate has the same defect (passed a directory, resolves first spec via readdir). The local runner is scoped correctly (migrate-local.ts findGeneratedSpec), so CI — declared the authoritative gate — diverges and is the buggy one. Compounding this, the report Source→Output headers are independently mis-wired: silent-conditionals.spec.ts.md → 'Output: nth-selectors.spec.ts', force-clicks.spec.ts.md and missing-await.spec.ts.md → 'Output: search-filters.spec.ts'. The numbers a reviewer reads to approve a file describe a different file.
- **Evidence:** migrate.yml:1220-1231 (`find outputs/tests -maxdepth 2 -name '*.spec.ts' … | head -1` then evaluate.ts --output "$OUT_SPEC" --report-out outputs/reports/<base>.md); migrate.yml:942 (ast-diff --output outputs/tests, dir) + ast-diff-trivial-check.ts resolves first spec; migrate-local.ts:573-579 scopes correctly (CI/local divergence); verified report headers: silent-conditionals.spec.ts.md says 'Output: nth-selectors.spec.ts', force-clicks.spec.ts.md and missing-await.spec.ts.md both say 'Output: search-filters.spec.ts'.
- **Source dimensions:** pipeline-determinism, stage2-output-defects
- **Verify verdict:** isReal=True sevConfirmed=5 — Independently confirmed every load-bearing claim by reading the cited files.\n\n1) CI scores the wrong spec — CONFIRMED. migrate.yml:1220-1221 sets OUT_SPEC=$(find outputs/tests -maxdepth 2 -name '*.spec.ts' -not -path '*/_legacy-v0.1.x/*' | head -1). The migrate job checks out ref: main (migrate.ym

### hallucinated-locators-ship-green — sev 5 [trustworthy-measured-quality]
**Hallucinated semantic locators ship as auto-passed output; the scorer rewards them and reports 100% canonical**

- **Problem:** Across all 5 bad-PW migrations the POMs translate CSS/index source selectors into INVENTED semantic locators with no evidence in the source: product-listing.page.ts getByTestId('cart-count'), getByRole('article'), getByTestId('cart-drawer'); cart.page.ts getByTestId('cart-subtotal'), getByLabel(/quantity/i); search-filters.page.ts getByRole('searchbox'). The source used .product-card, header>div nth, .cart-subtotal CSS — no testids, no roles. The model even writes 'ask FE to add data-testid' in comments. These tests cannot pass against the real app. This directly violates generate.md:202/234 ('Do NOT invent test IDs'; 'MUST NOT promote a MED/LOW locator to a hallucinated getByRole'). The scorer amplifies this: selectorQualityScore (evaluate.ts:224-228, 0.25 weight) is a pure surface-string count where a hallucinated getByTestId scores +1 canonical, identical to a verified one; and it returns 1.0 when total===0, so specs (0 locators) score a free perfect 1.00 on 25% of the weight. Reports print 'Selector quality: 100% canonical' while POMs (dashboard.page.ts) still carry live .locator('.welcome-banner')/.locator('.notification-item').first() that the smell table claims were removed. This is the dominant reason ~33% acceptable coexists with ~60% auto-ship.
- **Evidence:** product-listing.page.ts:16-50, cart.page.ts:21-29, search-filters.page.ts:15-37, search-filter.block.ts:11-45 (invented roles/testids, all 'Qn unresolved'); source inputs/bad-playwright/nth-selectors.spec.ts:19-36 + missing-await.spec.ts:14,24 (CSS only); generate.md:202,234 (prohibits inventing testids/promoting to hallucinated getByRole); evaluate.ts:224-228 (selectorQualityScore string-count, returns 1 when total===0), :455 (*0.25); dashboard.page.ts:31-45 (live .locator('.class')) vs silent-conditionals report smell row cssClassSelectors 3->0; measured-quality-baseline.md:84-88 (anti-correlation root cause).
- **Source dimensions:** stage2-output-defects, scorer-trust
- **Verify verdict:** isReal=True sevConfirmed=3 — I read every cited file. The CORE defect is real: the bad-PW POMs invent semantic locators with zero source evidence. Source (inputs/bad-playwright/nth-selectors.spec.ts:19,24,32; missing-await.spec.ts:14,24) uses pure CSS (.product-card, header>div nth, .cart-subtotal, .qty-input). Output product-l

### no-source-equivalence-gate — sev 5 [pipeline-engineering]
**No source-equivalence gate anywhere: a clean-but-behaviorally-wrong migration passes every check (assertion floor = 1)**

- **Problem:** No validator checks that the migrated test asserts the SAME behavior as the source. computeAggregateConfidence (evaluate.ts:446-462) uses only output-intrinsic signals plus the cap; the only source-vs-output check (isAstDiffTrivial) catches the opposite failure (verbatim copy). The assertion gate is ASSERTION_FLOOR=1 (evaluate.ts:304,324): a source with 8 assertions whose migration emits 1 passes, and the baseline doc markets this as 'no silent no-op' — a far lower bar than parity. The plan envelope carries expectedAssertions:string[] but it is declared and NEVER read (zero readers outside the interface). plan-code-coverage only checks a // plan:scenario= comment exists. semantic-regression-check.ts is a misnomer — it compares a NEW PLAN to a GOLDEN PLAN (anti-pattern/locator/high-med-low counts), never output-vs-source. So a migration that drops, weakens, inverts, or mis-targets assertions auto-ships. Outputs even INVENT brittle assertions absent from source (dashboard.page.ts NAV_LINK_COUNT=5 toHaveCount(5), name-pinned toContainText('Jane'), login.page.ts invented toBeHidden post-condition), creating false-negative flakiness the roulette proxy can't see (asserts live in POM methods across test.step boundaries).
- **Evidence:** evaluate.ts:446-462 (output-intrinsic + cap only), :362-376 (isAstDiffTrivial catches verbatim copy only), :304/:324 (ASSERTION_FLOOR=1, passed=emitted>=1); measured-quality-baseline.md:41 ('no migration is a silent no-op'); plan-code-coverage.ts:58 expectedAssertions declared, zero readers; semantic-regression-check.ts:2-9,380-437 (plan-vs-golden-plan); dashboard.page.ts:9-12,60-66,69-95 (invented toHaveCount(5)/name-pinned asserts), login.page.ts:49-54 (invented toBeHidden); roulette proxy blind to POM asserts evaluate.ts:188-195; AUDIT-96-PREP.md:34-37 (H4 conceded).
- **Source dimensions:** validator-coverage, scorer-trust, stage2-output-defects
- **Verify verdict:** isReal=True sevConfirmed=5 — Independently confirmed every load-bearing sub-claim by reading the cited files (canonical ./scripts and ./outputs, ignoring worktree copies).\n\n1) ASSERTION_FLOOR=1: evaluate.ts:304 `const ASSERTION_FLOOR = 1;` and :323-324 `passed: emitted >= ASSERTION_FLOOR`. The block comment (:298-303) explici

### human-acceptable-label-unauditable — sev 5 [trustworthy-measured-quality]
**The headline trust number (~33% human-acceptable) was LLM-generated, has no rubric, and is unreproducible — n=5 with no statistics**

- **Problem:** The trust number the whole 96% program is anchored to was NOT produced by a human and has no acceptance rubric. baseline.md:56-73 describes it as a 'multi-agent adversarial review (12 agents)' — LLMs judging LLM output — then calls it 'human-equivalent'. There is no written SHIP IT/FIX FIRST/START OVER rubric, no machine-readable verdict artifact (grep for verdicts in outputs/reports = none; zero -verify-sdet/-verify-code-review reports), and the verifications table in outputs/.metrics.db is EMPTY (verified COUNT=0). The verdicts exist only as a one-off hand-typed 6-row markdown table in one commit. The sample is also fatally small and inflated: COUNT(DISTINCT input_basename)=5 but COUNT(*)=14 (verified) — the 14 rows are the same 5 specs re-scored across 4 runs, and metrics-report.ts groups by COUNT(*) with no DISTINCT, over-reporting migration count ~3x. All 14 rows are bad-playwright (zero Cypress/Selenium). No CI/Wilson/significance/drift machinery exists anywhere. The same input scored 0.45→0.61 across runs (unmeasured ~0.16 variance), so a 0.7 gate decision is partly a coin flip. At n=5 a 60% rate has a 95% interval ~23–88% — cannot distinguish 33% from 96%.
- **Evidence:** measured-quality-baseline.md:56-73 ('12 agents','human-equivalent: 2/6 (33%)'),:76-82 (anti-correlation); sqlite3 verifications COUNT=0 (verified); sqlite3 migrations DISTINCT=5 COUNT=14, GROUP BY framework = bad-playwright|14 (verified); metrics-report.ts:184 COUNT(*) no DISTINCT; no wilson/binomial/CI/drift code in scripts/; search-filters re-scores 0.540/0.451/0.612; migrate.yml:1262 hard-coded 0.7 gate with no fit; AUDIT-96-PREP.md:25-26.
- **Source dimensions:** measurement-rigor
- **Verify verdict:** isReal=True sevConfirmed=5 — Independently confirmed every load-bearing sub-claim by reading the cited files and running the SQL myself.

PROVENANCE OF THE TRUST NUMBER (confirmed): docs/measured-quality-baseline.md:56-73 states the ~33% number came from a 'multi-agent adversarial review ... 12 agents' (LLMs judging LLM output)

### cypress-selenium-never-reach-output — sev 5 [multi-framework]
**Cypress & Selenium are example-status only: never carried to a Stage-2 spec, never measured; the 70% gate has zero datapoints**

- **Problem:** For Cypress and Selenium there is NO Stage-2 output and NO measured migration quality anywhere. outputs/tests/ contains only bad-PW specs (verified); outputs/plans/ has Stage-1 plans for Cypress (checkout-flow, wishlist) and Selenium (EmployeesTest, *JupiterTest, test_employees) but the pipeline was never carried through generation. The baseline only ever measures 'Step 1, bad-PW, n=5'. The framework-segmented artifacts that LOOK like coverage are not: tools/calibrate-pipeline conformance fixtures are hand-authored pwm-blueprint trees that test the deterministic validator, never invoking Claude (grep for anthropic|claude|sonnet returns nothing); the calibration corpus is reverse-engineered from the checks (fixtures carry inline '// Anti-pattern #N … Conformance Check N must block' comments), so 100% pass is guaranteed by construction. The only cross-framework LLM workflow (regression-semantic.yml) runs Stage-1 only and compares plan-shape to a golden plan. The selenium-java baseline-compare mixes in unrelated bad-PW POMs (no provenance link), benchmarks against a v0.1.x golden that itself violates pwm-blueprint hard rules, and always exits 0. There is no Cypress baseline at all. Promotion is gated on 70% acceptance that has literally never been measured for 3 of 4 frameworks.
- **Evidence:** verified outputs/tests = only bad-PW specs, outputs/plans has cypress/selenium plans+envelopes; measured-quality-baseline.md:24 ('Step 1, bad-Playwright, n=5'); calibrate-pipeline fixtures carry '// Anti-pattern #N … Conformance Check N must block' inline, run-calibration.ts spawns only validator scripts (no LLM); regression-semantic.yml runs analyze stage only, compares to expected-plan.md; baseline-compare-selenium-java.ts mixes bad-PW POMs + exits 0, golden selenium-java-03 login.spec.ts:12-17 imports from @playwright/test + new LoginPage(page) (hard-rule violations); package.json has only baseline:selenium-java; AUDIT-96-PREP.md:40.
- **Source dimensions:** multiframework-readiness, validator-coverage
- **Verify verdict:** isReal=True sevConfirmed=5 — Independently confirmed the load-bearing claims by reading the cited files. (1) outputs/tests/ contains ONLY bad-PW specs (force-clicks, search-filters, missing-await, silent-conditionals, nth-selectors); the only Selenium specs live in outputs/tests/_legacy-v0.1.x/ which its own README marks as arc

### rag-decorative-unmeasured — sev 5 [rag]
**RAG is decorative: STAGE1_RAG=off by default, zero shadow/on A/B runs ever recorded, retrieval value UNMEASURED**

- **Problem:** The full RAG apparatus is wired but gated behind STAGE1_RAG which defaults to off (verified plan.yml:556). In off mode plan.yml empties the rag-context fragment and skips retrieval, so no production plan is RAG-influenced. The load-bearing experiment (does injecting a retrieved plan improve the emitted plan?) has never run: rag-variance-report returns INSUFFICIENT-DATA (shadow=0 on=0) and there are zero *-rag.json sidecars (verified no matches). The project's own doc admits 'retrieval value in production is not yet measured.' The headline MAP@3=0.868 overstates skill: isRelevant marks docs relevant on framework-match OR shared KB-ID, and 82.8% of relevance pairs are explainable by framework alone; the query seeds the framework token and every doc is framework-tagged, so BM25 trivially matches a framework string to itself. The one query lacking the framework label (selenium-java-03, framework='unknown') scores AP@3=0.000. MAP is also computed only over the 16 goldens, not the 31-doc production index retrieval actually searches. The variance gate that would prove value is conjunctive over 3 noisy small-N metrics (PASS needs all stddevDelta<=0 AND drop>=1.0) and structurally biased to HOLD.
- **Evidence:** plan.yml:556 (STAGE1_RAG default off, verified), :573-577 (off empties fragment+exits); rag-variance-report INSUFFICIENT-DATA shadow=0 on=0; `ls outputs/reports/*-rag.json` = no matches (verified); rag-phase1.md:104-120 ('retrieval value in production not yet measured'); rag-map3-evaluator.ts:204-212 (isRelevant framework OR KB-ID), :274 (query seeds framework), selenium-java-03 AP@3=0.000; loadCorpus walks only examples/ while retrieval-bm25.ts reads outputs/.rag-index.json (31 docs); rag-variance-report.ts:178-203 (conjunctive HOLD-biased gate).
- **Source dimensions:** rag-quality
- **Verify verdict:** isReal=True sevConfirmed=5 — I independently confirmed the core of this claim by reading the cited files and running the evaluator.

CONFIRMED:
- plan.yml:556 `STAGE1_RAG: ${{ vars.STAGE1_RAG || 'off' }}` defaults off. Lines 573-577: in off mode the rag-context fragment is truncated (`: > prompts/_fragments/rag-context.md`) and

### ci-adoption-path-broken-out-of-box — sev 5 [adoptability]
**The advertised CI adoption path is broken out-of-the-box: bot PRs don't trigger gates, and onboarding routes users straight into it**

- **Problem:** The core sold path (push test → plan PR → merge → code PR with 'all gates passed') silently half-fails for every new user. plan.yml:983 and migrate.yml:1271 open PRs with peter-evans/create-pull-request using the default GITHUB_TOKEN, with NO create-github-app-token step anywhere in .github/workflows (verified 0). GitHub does not trigger downstream pull_request workflows from GITHUB_TOKEN-created PRs, so danger.yml and lint-output.yml never run on the bot code PR. The repo's own troubleshooting.md:201-247 documents this exact blocker ('PR sits at UNSTABLE indefinitely', fix = create a GitHub App + PWM_APP_ID/PWM_APP_PRIVATE_KEY), but pilot-kit.md:54-57 lists only CLAUDE_CODE_OAUTH_TOKEN and ANTHROPIC_API_KEY — the App is never mentioned. So a forker who follows pilot-kit verbatim gets stuck code PRs and the README's 'gates all passed' is a lie for them.
- **Evidence:** migrate.yml:1271 + plan.yml:983 (peter-evans create-pull-request v7.0.5, default token); grep create-github-app-token in workflows = 0 (verified); danger.yml/lint-output.yml are on: pull_request; troubleshooting.md:211-227 ('UNSTABLE indefinitely', fix = GitHub App + PWM_APP_ID/PWM_APP_PRIVATE_KEY, verified); pilot-kit.md:56-57 secrets table omits the App (verified, only OAUTH + API_KEY).
- **Source dimensions:** adoptability
- **Verify verdict:** isReal=True sevConfirmed=4 — Independently confirmed every load-bearing fact. (1) plan.yml:983 and migrate.yml:1271 both use peter-evans/create-pull-request@v7.0.5 with NO token: parameter — I grepped both files; the only token: lines are 'id-token: write' permissions, unrelated. So peter-evans defaults to GITHUB_TOKEN. (2) gre

### no-local-stage1-for-own-test — sev 4 [adoptability]
**No local way to plan your OWN test: local Stage-2 dead-ends on 'no approved plan', so there is no working end-to-end route for a new input**

- **Problem:** quickstart.md §4 tells a CI-averse adopter to run `npm run migrate -- --input <their test>` after `npm run try-it`, but migrate-local.ts:605 hard-errors 'no approved plan at <plan>' because Stage 2 requires a pre-existing outputs/plans/<base>.md. The only local plan-producing command is try-it.ts, which is hard-wired to the demo SAMPLE_INPUT and never reads --input. There is no npm run plan/analyze script (only try-it). So local Stage 2 produces a plan ONLY for the bundled demo; for the user's own test it always errors and points back to CI (plan.yml — itself blocked by the bot-PR and grounding issues). Combined with the broken CI path, a new adopter has NO working end-to-end route to migrate their own test. The --check doctor compounds this: it verifies Node>=22 and token PRESENCE only (not validity, not network, not that a plan can be produced), then prints 'Ready' before a run that will fail on 'no approved plan' or an expired token. The real first run also silently shells out to `npx @anthropic-ai/claude-code` (~30 MB download, agentic, --permission-mode acceptEdits) — undisclosed in Prerequisites.
- **Evidence:** migrate-local.ts:605 ('no approved plan … Run Stage 1 first', verified); try-it.ts hard-wired to SAMPLE_INPUT, never parses --input; package.json has only try-it, no local plan/analyze script; quickstart.md:109-131 instructs local Stage-2 as if a plan exists; migrate-local.ts:130-144 (--check = node>=22 + auth presence only, prints 'Ready'); try-it.ts:203-216 (spawns npx @anthropic-ai/claude-code, acceptEdits), :236 (~30 MB download in error path only).
- **Source dimensions:** adoptability
- **Verify verdict:** isReal=True sevConfirmed=3 — I independently read all cited files. The core mechanics are CONFIRMED: (1) migrate-local.ts:604-607 hard-errors "no approved plan at <plan> ... Run Stage 1 first (plan.yml, or `npm run try-it` for the demo)" when the plan file is absent. (2) migrate-local.ts never runs Stage 1 / never calls Claude 

### plan-confidence-keyword-scan — sev 4 [trustworthy-measured-quality]
**Plan confidence (40% — the heaviest weight) is a gameable prose keyword scan, not a measurement**

- **Problem:** planConfidence (evaluate.ts:335-356) is the single heaviest input (0.40 weight, verified evaluate.ts:454/491). It scans every markdown line with /\bhigh\b/ etc., first-match-wins, matching high/med/low ANYWHERE in the line — in prose, KB rationale, upgrade-path notes — not a confidence column. Real plans break it: force-clicks rows contain 'high' in prose ('Mechanical','unchanged') → aggregate ~1.0 maxing the 0.40 contribution; flaky-waits uses a warn/info/info severity column so its genuine confidence rows count as zero. Lines like 'raises selector quality from 0/1 to 1/1' and 'HIGH-confidence fallback' get counted. A Stage-1 model can inflate 40% of its downstream confidence purely by writing 'high' more often, and plans using a non-high/med/low vocabulary are scored as if they had no confidence data. This is the canonical gameable-scorer anti-pattern on the largest weight.
- **Evidence:** evaluate.ts:335-356 (regex /\bhigh\b/ first-match-wins per line, verified :345), :454 (aggregate*0.4), :491 (report weight 0.40); plan inspection: force-clicks rows 'high' in prose, flaky-waits severity column warn/info/info, FluentWaitJupiterTest.java.md 'HIGH-confidence fallback' + 'raises selector quality 0/1 to 1/1' as prose.
- **Source dimensions:** scorer-trust
- **Verify verdict:** isReal=True sevConfirmed=4 — Independently confirmed by reading scripts/evaluate.ts and replicating the scorer on real plans. evaluate.ts:335-356 scans every markdown table line and classifies via /\\bhigh\\b/ then /\\bmed\\b/ then /\\blow\\b/ first-match-wins, matching ANYWHERE in the line (prose, KB rationale, Evidence cell),

### scorer-scores-whole-shared-pom — sev 4 [pipeline-engineering]
**Scorer measures the whole shared POM (dead/inherited methods), so per-migration confidence is contaminated and drifts to 1.0**

- **Problem:** collectEmittedSources (evaluate.ts:633-679) unions WHOLE POM files (parts.push(src)), then selectorMix, web-first rate, smells, and the assertion floor all run over that union. But pwm-blueprint POMs are SHARED across migrations and accumulate methods over time. A migration is credited/penalised for code it never runs: missing-await.spec.ts calls 6 cartPage methods yet cart.page.ts contributes all 5 locators and every expect() in the file to the score. As shared POMs grow, selectorQuality drifts toward 1.0 for everyone and the assertion floor becomes trivially satisfiable by inherited methods, so the score grows progressively less discriminating. This couples to the determinism failure: the shared outputs/helper/ tree is never reset between migrations (migrate-local.ts:645-656 batch loop, no per-input reset; no add-paths on the PR), so one migration's POM edits ride into main and the NEXT inherits them. The baseline names this exact cross-migration POM contamination as Run-2 root cause (e.g. flaky-waits inherits an un-sourced expect(inputEmail).toBeHidden()). Same input + same plan can yield different shipped code depending on run order — history-coupled, non-deterministic output that already lowered real acceptance.
- **Evidence:** evaluate.ts:633-679 (unions whole POM), :625 (parts.push(src)), :728-736 (selectorMix/web-first/smells/assertion floor over union); missing-await calls 6 cartPage methods, cart.page.ts ships 5 locators scored wholesale; measured-quality-baseline.md:90-95 (cross-migration POM contamination, flaky-waits/silent-conditionals examples); migrate-local.ts:645-656 (batch loop no reset); migrate.yml:1271-1294 (create-pull-request, no add-paths → commits whole worktree).
- **Source dimensions:** scorer-trust, pipeline-determinism
- **Verify verdict:** isReal=True sevConfirmed=4 — Independently confirmed both halves by reading the cited code.

SCORER CONTAMINATION (confirmed): collectEmittedSources (evaluate.ts:633-679) resolves fixture-injected page objects via extractUsedFixtures→mapFixturesToFiles and pushes the WHOLE file (parts.push(src), line 625). main() then runs sele

### warn-checks-never-gate — sev 4 [pipeline-engineering]
**Half the conformance wall is warn-only and the pipeline runs without --strict, so brittle-CSS-fallback and POM-contamination checks never block**

- **Problem:** The conformance validator has 16 block + 17 warn emissions; in default mode it returns nonzero only on blockCount>0 (validate-pwm-blueprint-conformance.ts:1267-1268). The pipeline invokes it WITHOUT --strict (migrate-local.ts:383), so every warn check is non-blocking in production — including exactly the defect classes implicated in the 33% rate: W1 click-without-assertion, W2 try/catch-in-page (swallows flake), W5 bare CSS/XPath 'fallback' locator (the brittle-selector survival path), W15 POM-contamination (the measured Run-2 root cause). A migration riddled with these auto-ships while the report says 'conformance: clean'. The checks are also regex line-scanners blind to semantics: the expect-label check only verifies a [LABEL] string arg is present, accepting tautological expect(true,'[L]').toBe(true); there is no roulette/vacuous-matcher/wrong-target check. Form is enforced; meaning is not.
- **Evidence:** validate-pwm-blueprint-conformance.ts:1267-1268 (default returns blockCount>0; --strict returns violations>0), 16 block vs 17 warn; W1:538-544, W2:564, W5:646-651, W15:504-509 all severity warn; migrate-local.ts:383 (no --strict); Check 4 expect-label :336-359 (presence-of-bracket-string only); no roulette/toBeTruthy/not.toThrow checks; measured-quality-baseline.md:45 (surviving .product-card CSS + assertion-roulette in shipped output).
- **Source dimensions:** validator-coverage
- **Verify verdict:** isReal=True sevConfirmed=4 — Independently confirmed by reading the cited files. (1) validate-pwm-blueprint-conformance.ts:1259-1268 — default mode returns nonzero only on blockCount>0; --strict returns on violations.length>0. The validator's own header (lines 87-89) states "0 = clean (or only warn-severity in default mode)". (2) S

### offline-gate-blind-on-cypress-selenium-and-noop-on-pw — sev 4 [multi-framework]
**The offline abstention gate (default-active DOM grounding) is blind on Cypress/Selenium and a no-op on bad-PW — it catches nothing on the real corpus**

- **Problem:** The offline gate (validate-plan-dom-grounding.ts:448-463) is the ONLY grounding that runs when no SUT exists (the default), and it provides zero protection on the measured corpus. (a) It forms a LocatorPin only when a row has >=2 'locator-shaped' cells, where 'locator-shaped' is defined as only page.getBy*/page.locator (isLocatorExpression:342-347). Cypress/Selenium plans put NATIVE syntax in the Original column (cy.get(...), driver.find_element), which never matches, so the pin is silently dropped — all 4 real plans with HIGH rows exit 0. (b) Even on bad-PW, nameDerivableFromSource only inspects high/null rows and skips low/medium where real uncertainty lives (checkout-flow 0 HIGH/7 low/6 med; nth-selectors 8 low/1 med). (c) For bad-PW HIGH rows the source already contains the accessible names (getByRole name:'Sign in' etc.), so every HIGH name is trivially 'derivable' — which proves the LLM COPIED it, not that the element exists. CI shows a green 'offline abstention gate passed' that predicts nothing. Cross-language inputs also silently skip the live probe: dom-ground keys on the input basename (find outputs/tests/EmployeesTest.java) but Stage 2 emits kebab-case (employees.spec.ts), so the loop never iterates and dom_probe_status=pass with zero locators probed.
- **Evidence:** validate-plan-dom-grounding.ts:276-297 (pinFromTableRow <2 returns null), :342-347 (isLocatorExpression only page.getBy*/page.locator), :448-463 (validateOfflineAbstention), :423-429 (nameDerivableFromSource), :454-461 (skips low/medium); checkout-flow.cy.js.md Original cells are cy.get/cy.contains; all real HIGH-row plans exit 0; force-clicks HIGH names copied from inputs/bad-playwright/force-clicks.spec.ts:8-15; migrate.yml:1162 (find outputs/tests/${BASE}, BASE=input basename) + plan-envelope-validate.ts:143-147 (cross-language stem rename) → loop no-ops, dom_probe_status=pass.
- **Source dimensions:** dom-grounding-maturity, pipeline-engineering, pipeline-determinism
- **Verify verdict:** isReal=True sevConfirmed=4 — Independently confirmed all material sub-claims by reading the validator and running it on the real corpus.\n\n(a) BLIND ON CYPRESS/SELENIUM — CONFIRMED. isLocatorExpression (validate-plan-dom-grounding.ts:342-347) matches ONLY page.getBy*/page.locator. I grep-verified that all 5 cross-framework pla

### validator-runs-over-whole-tree — sev 3 [pipeline-engineering]
**The validator wall (tsc/eslint/playwright --list/envelope) runs over the entire accumulated tree, so a sibling's state non-deterministically passes/fails the current leg**

- **Problem:** Stage-2 validation is not scoped to the input being migrated: tsc -p outputs/tests/tsconfig.json (whole project), eslint over outputs/tests/**/*.ts, playwright test --list fed `find outputs/tests -name '*.spec.ts'` (ALL specs, migrate.yml:782-792,914-916), and plan-envelope-validate addSourceFilesAtPaths('**/*.spec.ts'). So if any previously-merged spec or a half-contaminated shared POM is currently broken, the current migration's wall goes red though ITS files are clean (false negative that blocks a good migration and burns Sonnet+Opus tokens on START-OVER); the inverse can mask a real defect. The wall verdict is a property of the whole repo, not of this migration, making pass/fail dependent on run order. The recovery loop compounds the non-determinism: a single transient Opus lens crash → START OVER, and the cheap retry fires via `gh workflow run` which 403s without actions:write and silently falls through to a full auto-regen (~10x cost per troubleshooting.md), so the SAME transient cause non-deterministically costs ~$0, ~10x, or a hard HALT.
- **Evidence:** migrate.yml:759 (tsc whole project), :770 (eslint outputs/tests/**), :782-792/:914-916 (playwright --list over all specs), plan-envelope-validate.ts:128 (**/*.spec.ts); migrate-local.ts:404-421 (listOutputSpecs whole tree); verify.yml:510-535 (missing/unparseable report → START OVER), :784-819 (retry via gh workflow run); troubleshooting.md:81-95 (403 without actions:write → ~10x full regen), :57-77 (Opus crash/SIGPIPE routine).
- **Source dimensions:** pipeline-determinism
- **Verify verdict:** isReal=True sevConfirmed=3 — Independently confirmed the core mechanism by reading the cited files. (1) The Stage-2 validator wall is unambiguously whole-tree, NOT scoped to the migrated input: migrate.yml:759 `npx tsc --noEmit -p outputs/tests/tsconfig.json`, :770 `npx eslint --fix 'outputs/tests/**/*.ts'`, :782-792 `mapfile -

### readme-oversells-vs-honest-baseline — sev 4 [adoptability]
**README leads with PROVEN/6⁄6/5⁄5/GREEN while burying the project's own 33%-acceptable / anti-correlated-scorer finding; prompt-tune ban cites a non-existent section**

- **Problem:** Adoptability is 'run it AND trust it', but the README a new evaluator reads (README.md:84-94) leads with 'end-to-end PROVEN', '6/6 plans', '5/5 outputs all confidence:high', 'DOM grounding 6/6 GREEN', '15 validators/100 fixtures GREEN'. The same-day update surfaces only the gentler 0.69/60% framing; the real headline — '2/6 (33%) SHIP IT' and 'scorer confidence is ANTI-correlated with real quality' from the maintainers' own baseline.md:73-81 — appears NOWHERE in README (grep for 33%/human-accept/anti-correlat = 0 hits). An adopter who later experiences the 33% loses trust in every other claim. Compounding the trust corrosion, the load-bearing debugging rule CLAUDE.md:94 ('do not prompt-tune — negative ROI, see ROADMAP "NOT to do"') cites a section that does not exist (grep 'NOT to do' = 0 matches) and has no empirical basis; AUDIT-96-PREP.md:52-54 concedes the stance 'may be wrong' and needs a controlled A/B never run. This forecloses the cheapest lever on Stage-2 quality (the generate prompt read verbatim every run) on an unverifiable citation.
- **Evidence:** README.md:84-94 (PROVEN/6-6/5-5/GREEN), :96-107 (only 0.69/60%); grep README for 33%/human-accept/anti-correlat = 0; measured-quality-baseline.md:73 (2/6 33% SHIP IT), :76-81 (anti-correlated); CLAUDE.md:94 (prompt-tune ban cites ROADMAP 'NOT to do'); grep 'NOT to do' = 0 matches; ROADMAP.md headings v0.3/v0.4/v0.5/v1.0/Beyond only; AUDIT-96-PREP.md:52-54 (H8).
- **Source dimensions:** adoptability, prompt-kb-coherence
- **Verify verdict:** isReal=True sevConfirmed=3 — Independently confirmed both sub-claims by reading the cited files. (1) README oversell: README.md:86 leads "The pipeline is end-to-end PROVEN on random public GitHub tests"; :88-92 front-load "6/6 ... solid Stage 1 plans", "5/5 ... all confidence:high", "DOM grounding ... 6/6 GREEN", "15 validators

## Part B — researched fixes (deduplicated)

### scorer-scores-whole-shared-pom [pipeline-engineering] · effort L
**Scope scorer to method-reachable POM slice + reset the shared-helper worktree per migration**

- **Root cause:** Two compounding root causes. (A) collectEmittedSources unions WHOLE shared-POM files (evaluate.ts:625, :673) and main() scores selector/web-first/smells/forbidden/assertion-floor over that union (evaluate.ts:724-736), so a migration is scored over inherited/dead methods it never runs — confirmed concretely by dashboard.page.ts carrying both force-clicks methods and a '// silent-conditionals migration' block while force-clicks' spec calls only 4 of its methods. (B) runBatch never resets outputs/helper between inputs (migrate-local.ts:645-656) and the CI PR step has no add-paths (migrate.yml:1268-1283), so one migration's POM edits commit into the branch and the next inherits them — run-order-dependent shipped code. Baseline (measured-quality-baseline.md:89-95) names this as a real acceptance-lowering Run-2 root cause.
- **Recommended solution:** Two complementary changes mapped to one indicator. PRIMARY (scorer): replace whole-file unioning with a method-reachability slice. The spec calls page-object methods by name on its fixture-injected objects (e.g. dashboardPage.expectDashboardURL — confirmed by grep). Build a per-POM reachable set: start from the methods the spec actually calls on each fixture object, then transitively include methods those bodies call via this.<method>() and the readonly locator fields they reference via this.<locator>. Score selectorMix/selectorQuality/webFirstRate/smells/forbidden/assertions over ONLY (a) the spec source plus (b) the reachable method bodies + their referenced locator declarations of each imported POM — not dead/inherited siblings. Non-POM helpers (test-data, utilities, types) stay whole-file (they have no per-migration method partition). This makes confidence measure the migration, not the file's accumulated history, and removes the bidirectional CSS-injection noise the verifier flagged. Keep the existing whole-tree path behind a flag for one release to compare scores. SECONDARY (determinism): make shipped output run-order-independent. (1) In runBatch, snapshot the outputs/helper tree before the batch and `git stash`/reset it to HEAD between each runOne so no migration inherits a prior iteration's uncommitted POM edits (or run each input from a clean worktree checkout). (2) In migrate.yml add `add-paths` to the peter-evans step listing exactly this migration's expected artifacts (the spec, its page-object(s), the base.fixture extension, its report) so the PR never commits another migration's drift. This is the higher-trust fix: the scorer change makes the number honest, the worktree-reset makes the shipped code deterministic.
- **Alternatives considered:** 1) Naively scope to ONLY the spec source (drop helpers entirely): rejected — pwm-blueprint deliberately hides locators/smells in POMs, so spec-only scoring would blind the scorer to the very signals it exists to measure (the comment at evaluate.ts:721-723 documents why whole-tree was chosen). Method-reachability keeps the POM signal while excluding dead code. 2) Split shared POMs into per-migration files (one POM per migration): rejected — defeats the pwm-blueprint shared-architecture goal and would explode the helper tree; the contamination is a scoring/determinism artifact, not an architecture flaw to undo. 3) Raise ASSERTION_FLOOR to compensate for inflated counts: rejected — the verifier correctly noted the floor angle is weakly material and the contamination is bidirectional noise, not monotonic inflation, so a higher floor wouldn't fix discrimination. 4) Only fix determinism (reset+add-paths), leave the scorer: rejected — leaves per-migration confidence non-discriminating as POMs grow, so the verify-fire gate keeps reading file history rather than the migration. Both halves are needed.
- **Concrete steps:**
  1. Add a pure function sliceReachablePom(pomSrc: string, calledMethods: Set<string>): string to scripts/evaluate.ts — parse method declarations (async \w+\s*\([^)]*\)\s*:\s*Promise) and readonly locator fields, compute the transitive closure over this.<method>() calls, collect referenced this.<locator> fields, and return only those declarations concatenated. Pure + unit-testable.
  1. Add extractCalledMethods(specSrc: string, fixtureName: string): Set<string> to evaluate.ts — regex \b<fixtureName>\.(\w+) per fixture object (reuse the same fixtureToFile mapping already built at evaluate.ts:646-650 to know which fixture maps to which POM file).
  1. Refactor collectEmittedSources (evaluate.ts:633-679): for files reached via the fixture map (path under page-object/pages/ or /blocks/), push sliceReachablePom(src, calledMethods[fixture]) instead of the whole src at line 625; keep whole-file push for non-POM helpers and keep the import-following for test-data/types. Make the stem-named union fallback (661-676) also slice when the matched file is a POM.
  1. Gate the new behavior behind env PWM_SCORE_SCOPE=reachable|whole (default reachable after a calibration run); leave the old union reachable via the flag for one release for A/B score comparison.
  1. Add fixtures to scripts test corpus: a POM with a dead inherited method block (model dashboard.page.ts) + a spec that calls only a subset, asserting the sliced score excludes the inherited CSS locators. Run npm run calibrate to confirm no regression on clean single-method POMs.
  1. Determinism (migrate-local.ts runBatch, ~645-656): before files.map, capture the helper tree state; between each runOne, restore outputs/helper to its pre-batch git state (git checkout/stash of outputs/helper) so iteration N+1 cannot inherit iteration N's uncommitted POM edits. Guard with a flag so single --input runs are unaffected.
  1. Determinism (.github/workflows/migrate.yml peter-evans step ~1268-1283): add an add-paths: block enumerating this migration's artifacts (outputs/tests/<kebab>.spec.ts, outputs/helper/page-object/pages/<name>.page.ts, outputs/helper/fixtures/base.fixture.ts, outputs/reports/<basename>.md, plus any plan-declared blocks) derived from steps.guard outputs, so the PR commits only the current migration's files.
  1. Update docs/measured-quality-baseline.md and ROADMAP to record the scope change and re-measure mean confidence on the n=6 batch under reachable-scope to confirm scores become more discriminating (expect force-clicks confidence to move now that silent-conditionals CSS no longer contaminates it).
- **Risk:** Method-reachability parsing is regex-based on TS, not a real AST — risk of under-slicing (missing a method called via a computed/aliased name, e.g. destructured or passed as a callback) which would drop real code from the score and falsely lower confidence, or over-slicing (matching a method name that is also a substring). Mitigation: prefer ts-morph (already a candidate per project risk-solutions memory) for the closure, and fall back to whole-file scoring (with a WARN) when the slice yields zero matched methods for a POM the spec demonstrably imports — never silently score an empty slice. Calibration risk: changing scope shifts the 0.7 verify-fire threshold's meaning, so the threshold must be re-checked against the n=6 batch before flipping the default (do NOT ship reachable-scope and keep 0.7 unexamined). Determinism worktree-reset risk: an over-aggressive reset could discard the base.fixture extension a migration legitimately needs to keep across its own multi-file output — scope the reset to BETWEEN inputs, never WITHIN a single runOne. add-paths risk: if the enumerated paths miss a plan-declared block/util, that file silently won't be committed — derive the list from the plan envelope, not a hardcoded set, and fail loudly if a generated file is outside the add-paths set.
- **Indicator delta:** +8 to +12 points toward 96%. The determinism half directly closes a named non-determinism hole (run-order-dependent shipped code) that the baseline ties to lowered acceptance — that is a core pipeline-engineering (robustness/determinism/failure-handling) defect, so fixing it is high-leverage for this indicator. The scorer-scoping half is partly trustworthy-quality, but here it serves pipeline determinism by making the gate-feeding confidence reproducible per input. Estimate is bounded below 96 because (a) measurement remains tiny (n=6) so the improvement can't be proven at scale yet, and (b) the worktree-reset + add-paths fixes the batch path but does not address other determinism sources (LLM sampling variance, the stem-named union fallback's directory-walk order). Net: removes the single largest history-coupling defect in the pipeline, moving the indicator meaningfully but not to 96 alone.

### warn-checks-never-gate [pipeline-engineering] · effort M
**Promote the four defect-class conformance checks (W1/W2/W5/W15) to a blocking "defect" tier on both pipeline paths, and close the vacuous-expect hole in Check 4**

- **Root cause:** The conformance validator has two severities (block/warn) and two modes: default returns nonzero only on blockCount>0 (validate-pwm-blueprint-conformance.ts:1268), --strict returns nonzero on any violation (1267). Both production invocations run WITHOUT --strict — migrate-local.ts:383 and migrate.yml:1014 — so all 17 warn checks are non-gating. That set includes exactly the four defect classes the measured-quality baseline blames for the 33% acceptable rate: W5 bare-CSS/XPath fallback (the surviving .product-card locator, measured-quality-baseline.md:44-46), W15 POM-contamination load-gate (the measured Run-2 root cause), W1 click-without-assertion, and W2 try/catch-in-page (swallowed flake). A migration carrying these auto-ships while the report reads "conformance: clean". The single existing escape hatch (--strict) is unusable as the fix because it would ALSO block 13 cosmetic/regex-fuzzy warns (W6 verb-prefix, W10 test-name, W13 kebab-case, etc.), several with known false positives — so nobody ever turns it on, leaving the defect checks permanently dark. Separately, the form-not-meaning gap: Check 4 (checkExpectLabel, 336-360) only tests for a bracket inside a 2nd string arg (/,\s*["'`][^"'`]*\[/), so the vacuous expect(true,'[L]').toBe(true) passes; the only assertion-quality signal anywhere is evaluate.ts assertionRoulette (183-195), a self-described "LOW-RECALL v0 proxy" that "never hard-blocks".
- **Recommended solution:** Introduce a third severity tier "defect" (between warn and block) for the four checks implicated in the measured failures — W1 click-without-assertion (~540), W2 try/catch-in-page (564), W5 bare-CSS/XPath locator (650), W15 load-gate-structural (508) — and make the default exit code gate on blockCount + defectCount > 0, while leaving the remaining 13 cosmetic/fuzzy warns (W3,W4,W6,W7,W8,W9,W10,W11,W12,W13,W14,Check-6 naming, utilities-coverage) non-blocking. This is surgical: it gates ONLY the defect classes with hard measured evidence, not style. Wire it on BOTH paths (migrate-local.ts AND migrate.yml — the claim under-states scope; CI has the same gap). Because W5 over-fires on legitimate .locator() (CSS sometimes unavoidable), pair the promotion with an inline-suppression escape valve: a // conformance-allow: W5 <reason> comment on the offending line downgrades that single hit back to warn, so an honest, justified CSS fallback (or a content gate the author defends) is not a hard block — this keeps determinism without false-positive lockout. Then close the semantic hole in Check 4 by adding a vacuous-matcher sub-check: flag expect(<literal>, ...).toBe(<literal>) / .toBeTruthy() on a constant and expect(...).not.toThrow() as a new "defect"-tier emission (label/meaning, not just label/presence). Keep --strict as the all-warns mode for local authoring.
- **Alternatives considered:** (1) Just pass --strict on both paths — rejected: it blocks all 17 warns including cosmetic/fuzzy ones (W6,W10,W13) and W5's known over-fire, guaranteeing false-positive lockouts with no escape valve, which is why --strict is currently dead. The tiered approach gates only evidence-backed classes. (2) Feed warn counts into evaluate.ts confidence instead of gating — rejected as the primary fix: it routes to verify rather than blocking, and the measured data shows the scorer is anti-correlated (H1), so lowering confidence on these is the weaker lever; a deterministic block is the right tool for deterministic defects (per CLAUDE.md debugging discipline). Could be added later as a secondary signal. (3) Replace the regex scanners with a ts-morph AST pass for true semantic checks (roulette subject-distinctness, wrong-target) — correct long-term direction (the code's own 'v1 upgrade' note at evaluate.ts:190) but XL effort; out of scope for closing THIS gating hole now. The recommended fix is the minimal change that makes the existing checks actually gate without breaking adoptability.
- **Concrete steps:**
  1. In scripts/validate-pwm-blueprint-conformance.ts, widen the Violation severity union from 'block'|'warn' to 'block'|'defect'|'warn' (find the `interface Violation` / `severity:` type ~line 100-140).
  1. Change the four defect-class emissions from severity:"warn" to severity:"defect": checkClickWithoutAssertion (W1, ~543), checkNoTryCatchInPage (W2, ~564), checkLocatorPriority (W5, ~650), checkLoadGateStructural (W15, ~508). Leave all other warn emissions unchanged.
  1. Add an inline-suppression read in those four functions: before pushing, scan the offending line (and the line above) for /conformance-allow:\s*W(1|2|5|15)\b/; if present, downgrade that single Violation to severity:"warn" and append the author's reason to the message. Implement once as a helper `applySuppression(line, prevLine, code, v)`.
  1. In main() exit logic (1259-1268): add `let defectCount=0;` tallied alongside blockCount in the loop (1260-1263); change the default return to `return (blockCount + defectCount) > 0 ? 1 : 0;` and keep `if (args.strict) return violations.length>0?1:0;`. Update the violation summary line (1264-1266) to print block/defect/warn counts.
  1. Update annotate() so 'defect' prints `::error::` (like block) in CI, not `::warning::`, so the GitHub annotation reflects the gating reality.
  1. Extend Check 4 (checkExpectLabel, 336-360) — or add a sibling checkVacuousExpect — to flag tautological assertions as severity:"defect": detect expect(<numeric|true|false|string-literal>) subjects combined with .toBe(<same-class-literal>)/.toBeTruthy()/.toBeDefined(), and any .not.toThrow(). Reuse extractExpectArgs (316-333) for the subject.
  1. Add the new tier + suppression to the header docblock (10-93) and the Exit codes section (88-90): '0 = clean or only cosmetic warn; 1 = block OR defect (or any warn in --strict)'.
  1. migrate.yml: the conformance step at 1014-1016 needs no flag change (defect now gates by default), but update the step's leading comment block (1005-1012) which still says 'type-prefix locator names (warn, not block)' to document the new defect tier so the gate's behavior is discoverable.
  1. Add fixtures under fixtures/pwm-blueprint-conformance/ (or wherever calibrate reads): a bad-NN page with a trailing-.click() method, a try/catch page, a .locator('.product-card') page, a content-gated waitForPageLoad page, and a good-NN counterpart for each with the // conformance-allow suppression — so npm run calibrate proves the gate blocks AND the escape valve works, zero tokens.
  1. Run npm run smoke + npm run calibrate to confirm: the four defect fixtures now exit 1, the suppressed variants exit 0, and the existing n=5 outputs/ tree still passes (or surfaces a real defect to fix — re-score per measured-quality-baseline.md:127-135 to confirm no regression in the committed corpus).
- **Risk:** see above
- **Indicator delta:** +4 to +6 points toward 96%. The pipeline already has a dense validator wall; the gap here is that ~half of it is decorative. Promoting the four evidence-backed defect classes converts measured failure modes (brittle CSS, POM contamination, swallowed flake, click-without-assert — the classes named in measured-quality-baseline.md:44-46 and Run-2 taxonomy) from advisory text into deterministic blocks on BOTH the local and CI paths, which is a robustness/failure-handling win — the core of the pipeline-engineering indicator. It is bounded below +6 because (a) it is a gating-coverage fix, not a new capability, and the suppression valve intentionally lets justified cases through; (b) the headline trust blocker remains the scorer-vs-human anti-correlation (H1) and unverified semantic equivalence (H4), which this does not touch; and (c) the new vacuous-expect check is low-recall. It removes a clear "reports say clean while shipping the measured defect" hole, which is disproportionately valuable for trust even though the point delta is modest.

### hallucinated-locators-ship-green [trustworthy-measured-quality] · effort M
**Scorer is hallucination-blind: unverified canonical locators score identical to verified ones; Stage 2 promotes them and writes false "fallback kept" comments**

- **Root cause:** The scorer measures surface locator SHAPE, not locator PROVENANCE. selectorMix string-counts getByRole/getByTestId without asking whether they were verified against a DOM or invented; a hallucinated canonical locator is indistinguishable from a real one and earns identical credit (and total===0 yields a free 1.0). The #228 grounding cap bolts a binary offline safety net on TOP of this but does not fix the blindness inside the 0.25 selector slice, so the moment DOM grounding lifts the cap, the anti-correlation mechanism returns. Compounding it, Stage 2 promotes MED/LOW locators to hallucinated canonical primaries while writing false 'fallback kept' comments, and no validator catches the lie.
- **Recommended solution:** Replace the binary, grounding-dependent hallucination signal with a graded, provenance-aware one inside the scorer, plus a validator for false fallback comments. Parse the existing 'Qn unresolved'/'Reviewer fallback'/'not confirmed'/'assumed' WHY-comments (40 present today) to classify each canonical locator as verified vs unverified; give unverified ones partial credit in selectorQualityScore so a fully-hallucinated POM scores ~0.5 not 1.00 even with DOM grounding on; stop returning a free 1.0 for a non-trivial emitted tree with zero locators; print verified/unverified/fragile in the report instead of a misleading '100% canonical'; and add scripts/validate-locator-provenance.ts to hard-fail comments that claim a fallback the code doesn't contain. This directly attacks the measured anti-correlation root cause while leaving the #228 offline cap intact as a backstop.
- **Alternatives considered:** (A) Leave the scorer alone and rely solely on the #228 cap + Opus-verify. Rejected: the cap is binary and only bites offline; the verifier itself notes 'the scorer cannot reward verified over hallucinated canonical locators, leaving Opus-verify as the sole gate' — and once DOM grounding is on (the stated #1 lever) the blind string-count returns at full weight. (B) Hard-fail any promoted-without-fallback locator in Stage 2 (validator-only, no scorer change). Rejected as sole fix: it would block faithful migrations where the promotion is legitimately the plan's choice, and it doesn't make the SCORE honest; the scorer change is what restores predictive validity. (C) Drop selectorQuality weight to 0 / remove the slice. Rejected: it throws away a real positive signal (verified canonical locators ARE better) and over-corrects; grading is better than deleting. (D) Have Stage 2 emit a structured machine-readable provenance manifest instead of parsing comments. Better long-term and worth a follow-up, but heavier (prompt + envelope schema change) and the comment markers already exist and are consistent (40 across outputs), so comment-parsing is the lowest-risk first step that fits THIS codebase now.
- **Concrete steps:**
  1. scripts/evaluate.ts:202-228 — extend SelectorMix to {canonicalVerified, canonicalUnverified, fragile}. Write a helper that walks the emitted source line-by-line, and for each canonical-locator match looks back at the contiguous comment block above it for an unverified marker (/Q\d+\s+unresolved/i, /Reviewer fallback/i, /not confirmed/i, /\bassumed\b/i); classify accordingly. Keep the existing flat regex counts for fragile.
  1. scripts/evaluate.ts:224-228 — rewrite selectorQualityScore: total = verified+unverified+fragile; return (verified + 0.5*unverified)/total; for total===0 return 0.5 only when countAssertions(emitted)>0 (a real tree with no locators is suspect), else 1.
  1. scripts/evaluate.ts:405-419 — add selectorUnverified to the Report interface and populate it in main() (line 728-753).
  1. scripts/evaluate.ts:482 (renderReport) — change the 'Selector quality' line to print verified / UNVERIFIED / fragile counts, and add a row to the confidence-breakdown table noting unverified-canonical penalty.
  1. Create scripts/validate-locator-provenance.ts (copy the dir-walk + parseArgs + ::error:: skeleton from validate-todo-discipline.ts:31-60). Rule: for any comment line matching /kept as fallback|CSS class locator kept|fallback/i, require the next non-comment locator assignment to contain an actual fallback (/\.locator\(\s*['\"`][.#\[]/ or /\.or\s*\(/). If absent, emit a violation (false fallback claim).
  1. package.json — add the new validator to the validate:all / smoke chain (mirror how validate-todo-discipline is invoked).
  1. scripts/validate-report-metrics.ts — extend the self-consistency check so report's printed verified/unverified counts must match a re-derivation (prevents the scorer and report drifting, the bug class behind the stale 0/0 reports).
  1. Add fixtures: extend the calibration corpus (fixtures/<validator>/good-NN, bad-NN) with one POM that promotes getByTestId WITHOUT fallback + lying comment (bad) and one with a real .or() fallback (good), so the new validator and scorer split are pinned and never need a cloud rerun.
  1. Re-run npm run calibrate to confirm the n=5 bad-PW corpus: the hallucinated-heavy migrations (product-listing, cart) should now score materially below the verified ones on the selector slice, and the false-WHY-comment in cart.page.ts:21-29 / missing-await output should hard-fail the new validator.
- **Risk:** Primary risk is calibration/false-positives: the 0.5 partial-credit weight is a heuristic that cannot be properly fitted on n=5/6, and the provenance regex depends on Stage 2 keeping the 'Qn unresolved' comment convention — a locator promoted with NO comment would wrongly read as verified, so it must default to unverified when ungrounded. The false-fallback validator must anchor tightly to the 'kept as fallback' claim shape to avoid flagging incidental uses of the word. Critically, this fixes the scorer's BLINDNESS to hallucination, not hallucination itself — real locator validity still needs DOM grounding; do not oversell.
- **Indicator delta:** Estimate +8 to +12 points toward 96% on trustworthy-measured-quality. The measured anti-correlation root cause (baseline.md:81 'the static scorer rewards surface locator quality and cannot see the defects that actually matter'; :84-88 hallucinated locators are the dominant FIX-FIRST defect) is exactly what this targets WITHIN the score, not just via the binary cap. The #228 grounding cap already removed the offline auto-ship pathway (so the catastrophic 'ships green' is closed — that's why this is sev-3 not sev-5), but it left two open holes this closes: (a) once DOM grounding is ON — the project's stated #1 quality lever (baseline:108) — the 0.69 cap lifts and the hallucination-blind 0.25 string-count slice is back at full strength with nothing distinguishing verified from invented canonical locators; (b) Stage 2 currently ships demonstrably false 'CSS fallback kept' comments (baseline:99-100, cart.page.ts:21-29) that no gate catches. After this fix the scorer grades verified vs unverified canonical locators continuously and the report stops printing the misleading '100% canonical', materially improving predictive validity of the score. It does NOT reach 96% alone: the remaining gap is the absent acceptance rubric / CIs (H2) and semantic-equivalence-to-source verification (H4), which are separate workstreams.

### human-acceptable-label-unauditable [trustworthy-measured-quality] · effort L
**Build an auditable acceptance-label corpus + Wilson-interval calibration harness, and fix the 2.8x migration over-count in metrics-report.ts**

- **Root cause:** The entire "96% ready" program is anchored to a single trust number (~33% human-acceptable) that is (a) LLM-generated, not human (measured-quality-baseline.md:56-73 calls it a "multi-agent adversarial review ... 12 agents" yet labels the output "Human-equivalent"); (b) rubric-less (no written SHIP IT/FIX FIRST/START OVER definition exists anywhere except as a future TODO in AUDIT-96-PREP.md:26 "define an acceptance rubric"); (c) unreproducible — it lives only as a hand-typed 6-row markdown table (measured-quality-baseline.md:63-70), with zero machine-readable verdict artifacts (no outputs/reports/*-verify-sdet.md / *-verify-code-review.md exist; the verify.yml persistence path at verify.yml:702 never ran for this batch, so the verifications table is empty: COUNT=0); (d) statistically void — n=5 distinct inputs, no CI/Wilson/binomial code exists in scripts/ (grep returns nothing), and the same input swung 0.45->0.61 across runs (~0.16 variance straddling the 0.7 gate). On top of this, the reporting code triple-counts the corpus: metrics-report.ts:176 and :184 use raw COUNT(*) (=14) instead of COUNT(DISTINCT input_basename) (=5), because the 14 rows are 5 specs re-scored across ~4 runs (search-filters alone has 5 rows). All 14 rows are bad-playwright (GROUP BY source_framework = bad-playwright|14); zero Cypress/Selenium ever entered the measured corpus, so the framework table that gates promotion is built on a single framework.
- **Recommended solution:** Replace the unauditable number with a reproducible measurement pipeline in three layers that reuse the existing MetricsDB + verify-report plumbing. (1) RUBRIC + LABEL ARTIFACT: write docs/acceptance-rubric.md defining a binary human verdict (ACCEPTABLE / NOT-ACCEPTABLE) with explicit reason codes mapped to the existing failure taxonomy already named in measured-quality-baseline.md:84-100 (hallucinated-locator, pom-contamination, optional-as-mandatory, false-why-comment, plus assertion-roulette/brittle-css). Store human labels as a committed, machine-readable JSONL corpus at labels/acceptance.jsonl (one record per migration: input_basename, framework, commit_sha, verdict, reason_codes[], labeler, dom_grounded:boolean, scorer_confidence). This is the new source of truth — NOT an LLM. The 12-agent review can stay as a CHEAP PRE-FILTER that proposes a verdict, but a human must confirm/override each row before it lands in the JSONL (the file records labeler='human'). (2) CALIBRATION HARNESS: add scripts/acceptance-calibrate.ts that joins labels/acceptance.jsonl against the migrations table on (input_basename, commit_sha), and emits: n (DISTINCT inputs), acceptable rate, Wilson 95% interval, and a confidence->acceptance reliability curve (bin scorer_confidence, show empirical acceptance per bin) plus the threshold that maximizes Youden's J — i.e. the data-fitted replacement for the hard-coded 0.7 gate (migrate.yml:1262/:1330). Add a tiny pure stats module scripts/lib/binom.ts (wilsonInterval(k,n) and a binomial point estimate) so the interval math is unit-tested. (3) FIX THE OVER-COUNT: change metrics-report.ts:176 and :184 to COUNT(DISTINCT input_basename) and add a separate 'runs' (raw COUNT(*)) column so re-scores are visible but never inflate the migration count; mirror the same DISTINCT fix in dashboard.ts and metrics-export.ts which the code comments say must agree. Gate honesty in CI: extend scripts/validate-report-metrics.ts to fail if any quoted "%-acceptable" in docs/ lacks an accompanying n and Wilson interval, and to fail if n(DISTINCT) < 20 while a single-number quality % is asserted (enforces the ADR-0001/baseline.md:146 ">=20 migrations" rule that is currently only prose).
- **Alternatives considered:** Rejected: (a) just enlarging the sample by running more migrations — does not fix the rubric-less, LLM-as-oracle, no-CI core problem; you'd get a bigger unauditable number. (b) Keeping the 12-agent adversarial review as the verdict source but calling it honestly 'LLM-judged' — still anti-correlation-prone (the scorer itself was anti-correlated per baseline.md:76) and not the human-acceptable ground truth the 70% promotion gate needs. (c) Only fixing the COUNT(*) bug — necessary but cosmetic; the headline number stays unauditable. The chosen layered fix is the minimum that makes the number reproducible (committed JSONL + rubric), honest (Wilson CI, DISTINCT counts), and self-policing (CI gate on unqualified % claims), while reusing the existing MetricsDB join keys and verify-report plumbing rather than building parallel infrastructure.
- **Concrete steps:**
  1. 1. Write docs/acceptance-rubric.md: binary ACCEPTABLE/NOT-ACCEPTABLE verdict, decision boundary, and a fixed reason-code enum (hallucinated-locator, pom-contamination, optional-as-mandatory, false-why-comment, assertion-roulette, brittle-css, semantic-drift) reusing the taxonomy already in docs/measured-quality-baseline.md:84-100. State explicitly: the label is HUMAN; LLM review is only a proposer.
  1. 2. Create labels/acceptance.jsonl with one record per migration {input_basename, framework, commit_sha, verdict, reason_codes, labeler, dom_grounded, scorer_confidence}. Seed it by HUMAN-confirming the 6 Run-2 rows currently in measured-quality-baseline.md:63-70 (verdict mapped FIX FIRST/START OVER -> NOT-ACCEPTABLE, SHIP IT -> ACCEPTABLE), each tagged labeler accordingly and dom_grounded:false.
  1. 3. Add scripts/lib/binom.ts: pure wilsonInterval(k, n, z=1.96) returning {lo, hi, point} and a unit test scripts/lib/binom.test.ts covering k=2,n=6 (must show ~0.06-0.71) and k=3,n=5.
  1. 4. Add scripts/acceptance-calibrate.ts: load labels/acceptance.jsonl, JOIN migrations on (input_basename, commit_sha) for scorer_confidence, print n(DISTINCT), acceptable rate + Wilson interval, per-confidence-bin empirical acceptance (reliability curve), and the Youden-J-optimal threshold vs the current 0.7. Add npm script 'calibrate:acceptance'.
  1. 5. Fix metrics-report.ts: line 176 -> (SELECT COUNT(DISTINCT input_basename) FROM migrations) AS migrations; line 184-189 -> SELECT source_framework, COUNT(DISTINCT input_basename) AS count, COUNT(*) AS runs, AVG(...) GROUP BY source_framework. Render both 'count' and 'runs' columns. Update the report unit test in scripts/validate-report-metrics.test.ts (or add one) asserting count=5, runs=14 on the current DB.
  1. 6. Apply the identical DISTINCT fix to scripts/dashboard.ts and scripts/metrics-export.ts (their comments at metrics-report.ts:115 say the three must agree).
  1. 7. Investigate why verifications COUNT=0: the Run-2 batch produced no outputs/reports/*-verify-*.md, so persist-verify-metrics.ts (verify.yml:702) never fired. Document in docs/troubleshooting.md that any quoted acceptance number is invalid unless the corresponding verifications rows exist; have acceptance-calibrate.ts warn when a labeled input has no matching verifications row.
  1. 8. Extend scripts/validate-report-metrics.ts to fail CI when a docs/ file quotes an X%-acceptable/auto-ship figure without an adjacent 'n=' and Wilson interval, and when n(DISTINCT) < 20 accompanies a single-number quality claim. Wire into npm run smoke / validate:all.
  1. 9. Rewrite the headline in docs/measured-quality-baseline.md:56-73 to quote the rubric-backed number WITH its Wilson interval and n(DISTINCT), and link labels/acceptance.jsonl + docs/acceptance-rubric.md as the provenance.
- **Risk:** Single-labeler at n=5-6 is still noise; require >=2 labelers + Cohen's kappa or 'human-labeled' is just another unaudited oracle. Youden-J threshold on n=5 overfits — harness must DESCRIBE not RECOMMEND a new gate below n=20. DISTINCT fix visibly drops reported count 14->5 (looks like regression; flag in CHANGELOG as a correction). CI %-claim regex must be scoped to acceptance/auto-ship keywords to avoid tripping on RAG MAP@3 percentages. Re-label only AFTER grounding-cap #228 is live, else the corpus measures the pre-fix world.
- **Indicator delta:** Large, foundational: this is the load-bearing item for indicator (2). Today the indicator is effectively unmeasurable (the number is LLM-made, rubric-less, n=5, no CI, triple-counted) — call it ~20-25% honest-ready. Completing steps 1-9 establishes a reproducible rubric, a committed human-labeled artifact, correct DISTINCT counts, and Wilson intervals on every claim — moving the indicator to roughly 60-65% toward 96%. It cannot reach 96% by itself: the remaining ~30 points require executing the harness at scale (n(DISTINCT) >= 20 with >=2 labelers and kappa reported, per the risk note) and demonstrating the fitted gate actually predicts acceptance on a held-out set. So estimate +35-40 points of the indicator (from ~22% to ~60%), with the final stretch gated on data collection this harness enables rather than more code.

### ci-adoption-path-broken-out-of-box [adoptability] · effort M
**Wire actions/create-github-app-token into plan.yml + migrate.yml and document PWM_APP_ID/PWM_APP_PRIVATE_KEY in onboarding, so bot PRs actually trigger the gates**

- **Root cause:** Both PR-opening steps (plan.yml:983, migrate.yml:1271) call peter-evans/create-pull-request@v7.0.5 with NO token: parameter, so they default to the workflow's GITHUB_TOKEN. By GitHub's anti-recursion security model, PRs created by GITHUB_TOKEN do NOT trigger downstream on: pull_request workflows. danger.yml (on: pull_request/pull_request_review) and lint-output.yml (on: pull_request paths outputs/tests/**) are therefore created as check runs but never execute any job — the bot code PR sits at mergeStateStatus UNSTABLE with both checks stuck action_required forever. The repo already knows the exact fix: docs/troubleshooting.md:215-234 prescribes actions/create-github-app-token@v1 + PWM_APP_ID/PWM_APP_PRIVATE_KEY repo secrets. But grep proves those identifiers exist in EXACTLY ONE file (troubleshooting.md) — zero workflows use them, and neither onboarding doc mentions them: pilot-kit.md:54-57 lists only CLAUDE_CODE_OAUTH_TOKEN + ANTHROPIC_API_KEY, README.md:200-202 lists only CLAUDE_CODE_OAUTH_TOKEN + MIGRATION_TARGET_URL. So the documented cure is never applied by default and a forker following pilot-kit/README verbatim hits the buried failure with no signpost. No auto-wake exists either (grep for allow-empty / synchronize-trigger in workflows = none). Net: the advertised CI path silently half-fails for every new user; PR-policy enforcement (danger rules, output lint) is ABSENT rather than falsely green.
- **Recommended solution:** Make the GitHub-App token the default identity for both bot PRs, with graceful fallback to GITHUB_TOKEN when the App secrets are absent, and surface the requirement at every onboarding touchpoint plus the zero-token doctor. (1) Add an actions/create-github-app-token@v1 step immediately before each peter-evans step in plan.yml and migrate.yml, gated on the App secrets being present, and pass its output as token: to peter-evans — falling back to github.token when the App isn't configured (so behavior degrades to today's known-limited state instead of erroring). (2) When the fallback path is taken, emit a ::warning:: and append a one-line banner to the PR body ('PR-policy gates danger + lint-output will NOT run on this bot PR until PWM_APP_ID/PWM_APP_PRIVATE_KEY are set — see docs/troubleshooting.md') so the gap is visible in the PR itself. (3) Add PWM_APP_ID/PWM_APP_PRIVATE_KEY to the pilot-kit.md secrets table and README Quickstart prerequisites with a one-sentence why and a pointer to the troubleshooting recipe. (4) Teach the zero-token doctor (scripts/migrate-local.ts --check) and quickstart-check.ts to detect whether the App is configured and print a WARN with the fix. This keeps the repo working out-of-box for solo experiments (fallback) while making the correct gate-firing setup the documented default and impossible to miss. Prefer the App over a PAT (troubleshooting.md:238 explains PAT identity/scope downsides) and never pull_request_target (troubleshooting.md:239 — supply-chain footgun for untrusted LLM-generated PRs).
- **Concrete steps:**
  1. In /Users/kapusansky/DEV/PWmodernizer/.github/workflows/plan.yml, immediately before the 'Open plan PR' step (line 981-983), add a step `- name: Mint App token (if configured)` id app_token with `if: steps.dedup.outputs.skip == 'false' && env.PWM_APP_ID != ''`, using actions/create-github-app-token@<pinned-sha> with app-id ${{ secrets.PWM_APP_ID }} and private-key ${{ secrets.PWM_APP_PRIVATE_KEY }}; hoist PWM_APP_ID into the step's env: so the if: can read it (secrets.* are not allowed directly in if:).
  1. In plan.yml, add `token: ${{ steps.app_token.outputs.token || github.token }}` to the with: block of the peter-evans/create-pull-request step (line 983-984).
  1. In plan.yml, append a conditional banner to the PR body: (after Caveats ~line 1018) when no App token was minted, include a line warning that danger + lint-output gates will not run until PWM_APP_ID/PWM_APP_PRIVATE_KEY are set (reference docs/troubleshooting.md), plus a ::warning:: echo step under the same condition.
  1. In /Users/kapusansky/DEV/PWmodernizer/.github/workflows/migrate.yml, mirror the App-token step immediately before the 'Open code PR' step (line 1268-1271), gated on steps.budget_guard.outputs.over_budget != 'true' && env.PWM_APP_ID != ''; add `token: ${{ steps.app_token.outputs.token || github.token }}` to the peter-evans with: block (line 1272).
  1. In migrate.yml, keep the accurate '### Validation gates (all passed)' header (line 1289 — those are in-job validators) but add a Caveats note that PR-policy gates (danger, lint-output) require the App token, mirroring plan.yml, so the wording isn't misread as covering PR-policy checks.
  1. Pin actions/create-github-app-token to a commit SHA (repo pins all third-party actions, e.g. peter-evans at plan.yml:983) and add it to any dependabot/allow-list under .github/; check dangerfile.ts doesn't reject the new action.
  1. In /Users/kapusansky/DEV/PWmodernizer/docs/pilot-kit.md, extend the 'Set repository secrets' table (lines 54-57) with PWM_APP_ID and PWM_APP_PRIVATE_KEY rows ('Required for danger + lint-output gates to run on bot PRs — see docs/troubleshooting.md') and add a Phase-1 checklist bullet.
  1. In /Users/kapusansky/DEV/PWmodernizer/README.md Quickstart prerequisites (lines 199-202), add PWM_APP_ID/PWM_APP_PRIVATE_KEY as recommended secrets with a one-line why and a link to docs/troubleshooting.md:215.
  1. In /Users/kapusansky/DEV/PWmodernizer/scripts/migrate-local.ts (--check doctor) and scripts/quickstart-check.ts, add a check that warns when PWM_APP_ID/PWM_APP_PRIVATE_KEY are not detected, explaining the CI code PR will sit UNSTABLE with PR-policy gates stuck action_required, and pointing to the fix; word it as 'could not confirm' since it can only read env locally, not repo secrets.
  1. Run npm run smoke and actionlint (if available) to confirm both workflows parse; verify the || fallback yields github.token when the App step is skipped (skipped step outputs = empty string, falsy in GHA).
- **Risk:** Main risk is the if: env.PWM_APP_ID != '' pattern: GHA cannot reference secrets.* directly in a step-level if:, so the App-ID must be hoisted into env: first — get this wrong and the gate always or never fires. Mitigate by env: PWM_APP_ID: ${{ secrets.PWM_APP_ID }} at the step and testing both states. The ${{ steps.app_token.outputs.token || github.token }} fallback must be validated to yield github.token when the App step is skipped (skipped-step outputs are empty string, falsy — correct, but verify). Pinning create-github-app-token to a SHA is mandatory (repo policy; check dangerfile.ts doesn't trip on the new step). Low false-positive risk: the change is additive and degrades to today's behavior when the App is absent, so no existing forker is made worse. Calibration note: the doctor warning is heuristic locally (it can only read env vars, not actual repo secrets), so word it as 'could not confirm' rather than asserting the secret is missing. Does not touch migration OUTPUT quality, so no regression risk to generated artifacts.
- **Indicator delta:** +6 to +9 points toward 96%. This is the single highest-leverage adoptability fix: it converts the headline sold path (push → plan PR → merge → gated code PR) from silently-broken to working-by-default for any forker who follows pilot-kit/README verbatim, AND removes the trust-eroding gap where advertised PR-policy gates are simply absent. The onboarding-doc omission was the actual trap (the code fix existed only in troubleshooting.md); closing it at the workflow + both onboarding docs + the doctor means a new user no longer needs to independently discover the failure. Capped below ~10 because it is one of several adoptability gaps (Node-22/auth friction, 'approved plan must exist first' ordering, error-message clarity per H9) and because the fallback still leaves un-App'd forkers in the degraded state — now with a loud warning, which is the honest ceiling for a tool that cannot provision a GitHub App on the user's behalf.

### rag-decorative-unmeasured [rag] · effort L
**Make RAG load-bearing: run the shadow→on A/B offline so retrieval value is measured, and fix MAP@3 so it reflects production retrieval instead of trivial framework self-matching**

- **Root cause:** The full RAG apparatus is wired but never exercised, and the one quality proof it does emit is misleading. Three independent root causes: (1) The load-bearing experiment — does injecting a retrieved plan improve the emitted plan? — requires real Sonnet token spend (STAGE1_RAG=shadow baseline vs =on, N>=5 each) and so has never been run; STAGE1_RAG defaults off (.github/workflows/plan.yml:556, verified), off-mode empties prompts/_fragments/rag-context.md and exit 0s before retrieval (plan.yml:573-577), and shadow-mode writes the sidecar but still keeps the fragment empty (the on-arm never runs in CI). Zero *-rag.json sidecars exist, rag-context.md is 0 bytes, and rag-variance-report returns INSUFFICIENT-DATA at shadow=0/on=0. (2) The quality proof is measured on the wrong surface and is dominated by a trivial signal. rag-map3-evaluator.ts loadCorpus (:183-202) walks ONLY examples/ and yields 16 golden queries — I reran it: 'Queries evaluated: 16 ... MAP@3: 0.868'. But production retrieval (retrieval-bm25.ts DEFAULT_INDEX :43) searches outputs/.rag-index.json, which I inspected: 31 docs (15 outputs + 16 examples), so the headline number never touches 15 of the 31 production docs. rag-phase1.md:106 mis-describes 0.868 as a '31-doc corpus' result, directly contradicted by the evaluator's own '16 queries' line. The metric is also dominated by framework self-matching: isRelevant (:204-212) returns true on framework-match OR shared KB-ID, buildQueryFromDoc seeds doc.framework into the query (:274), and buildIndex prepends d.framework to every doc's tokens (:222-226) — so BM25 matches a framework string to an identical framework string. 15/16 queries carry a clean framework label; the lone framework='unknown' query (selenium-java-03, the closest analogue to a real cross-framework retrieval) scores AP@3=0.000 (verified in the rerun). (3) The variance gate that would certify value (rag-variance-report.ts deriveVerdict :163-204) is structurally biased to never PASS: INSUFFICIENT-DATA below minSamples, HOLD on ANY null per-metric variance OR ANY single positive stddevDelta, PASS only if totalDrop>=1.0 AND every per-metric delta<=0 — a conjunctive gate over 3 noisy small-N integer metrics — and it is an orphan command (rag:variance is in package.json:71 but in NO .github/workflows/ job and NOT in the smoke chain), so nothing ever triggers it. CORRECTION to the warm-start claim: the production index is NOT all framework='unknown' — index-plans.ts parses '## Source framework' per plan, giving 11 bad-playwright / 7 cypress / 7 selenium-java / 4 selenium-python / 2 unknown. So the framework arm DOES exist in production; the defect is that MAP@3 evaluates the 16-doc golden subset, not the 31-doc production surface, and leans on a signal (framework self-match) that proves nothing about cross-framework retrieval — the actual hard case this RAG was built for (a Cypress cy.wait plan retrieving a Selenium Thread.sleep plan via shared KB IDs).
- **Recommended solution:** Make retrieval value measurable without requiring a human-present token-spend gate, and make the existing quality proof honest. See concreteSteps for the ordered plan; the three workstreams are (A) an offline ablation harness scripts/rag-ablation.ts that runs Stage-1 twice per golden (rag-off vs rag-on) and scores both against expected-plan.md via the existing semantic-regression-check axes with a paired CI — this is the load-bearing 'does retrieval improve the plan' test the rag indicator requires; (B) fix MAP@3 provenance to cover the 31-doc production surface and add a KB-ID-only (framework-arm-disabled) relevance number that honestly measures the cross-framework bridge; (C) de-bias rag-variance-report's conjunctive gate to a single paired statistic and wire it into CI instead of leaving it an orphan command.
- **Alternatives considered:** Cheaper alternative: skip the generation ablation and ship only the MAP@3-provenance + variance-gate fixes (B+C). Rejected as insufficient — MAP@3 measures retrieval RANKING, never whether the retrieved plan changes Sonnet's output, so it can never satisfy the rag indicator ('retrieval actually improves plans'); the ranking fixes are necessary hygiene but not the proof. Heavier alternative: embeddings / vector DB to improve recall. Rejected for now — ADR-0001 deliberately scoped Phase 1 to BM25, and there is no evidence BM25 recall is the bottleneck; you cannot justify embeddings before the off-vs-on ablation shows BM25 retrieval helps at all. Also considered gating the ablation in CI on every PR — rejected because it spends Sonnet tokens; the right shape is an operator-run, committed, replayable report (matching the repo's zero-token-by-default discipline in CLAUDE.md), with only the cheap deterministic checks (MAP@3 gate, variance report over the committed data) in CI.
- **Concrete steps:**
  1. scripts/rag-map3-evaluator.ts: in loadCorpus (lines 183-202) add the 15 outputs/ docs from outputs/.rag-index.json to the evaluated corpus (or add a --surface=production flag that reads the real index), so the gated MAP@3 covers the 31-doc surface retrieval actually searches, not just the 16 goldens.
  1. scripts/rag-map3-evaluator.ts: add a second relevance mode to isRelevant (lines 204-212) that disables the framework arm (KB-ID-only) and report BOTH MAP@3 numbers in renderMarkdown (367-386) and the JSON. The KB-ID-only number is the honest cross-framework retrieval score; flag if selenium-java-03's 0.000 generalizes.
  1. docs/rag-phase1.md:106: change '0.868 over the 31-doc corpus' to '0.868 over 16 golden queries (held-out leave-one-out)'; the evaluator's own output says 'Queries evaluated: 16' — fix the contradiction. Add a line noting the framework-inclusive vs KB-ID-only split.
  1. Create scripts/rag-ablation.ts: for each examples/<dir> with input.* + expected-plan.md, run Stage-1 generation twice (rag-context.md populated via retrieval-bm25 + rag-context-render = 'on', vs empty = 'off'), score each emitted plan against expected-plan.md by importing semantic-regression-check.ts's existing axes (KB-ID coverage, anti-pattern recall, locator-confidence distance), and emit a paired on-minus-off delta per axis with a bootstrap CI / paired sign test. Write outputs/reports/rag-ablation.json + a markdown summary.
  1. Document rag-ablation as the operator-run, one-time, token-spending A/B in docs/rag-phase1.md (replacing the 'requires real token-spending runs ... keep STAGE1_RAG=off' hand-wave at lines 115-120 with a concrete `npm run rag:ablation` command and an acceptance threshold, e.g. on must beat off on KB-ID coverage by >=+0.05 with CI excluding 0).
  1. scripts/rag-variance-report.ts deriveVerdict (163-204): replace the conjunctive 'every stddevDelta<=0 AND totalDrop>=1.0' rule with a single paired statistic over the per-run metric values (Wilcoxon signed-rank or bootstrap CI on the mean stddev delta); keep INSUFFICIENT-DATA below minSamples but stop returning HOLD on a single noisy positive delta.
  1. package.json: add `rag:ablation` script; add a non-blocking CI job (e.g. in regression-semantic.yml) that runs `npm run rag:variance` / surfaces the committed rag-ablation.json so the experiment appears in PRs — today rag:variance (package.json:71) is in NO workflow and not in smoke.
  1. Once rag-ablation shows on>off with CI excluding zero, change .github/workflows/plan.yml:556 default to STAGE1_RAG=on (or document the measured uplift in docs and ROADMAP). Until then the report itself is the deliverable that moves the rag indicator.
- **Risk:** The ablation could show retrieval does NOT improve plans (on ~= off, or on worse) — that is the honest outcome the rag indicator must surface, not a failure of the fix; it would justify keeping STAGE1_RAG=off and re-scoping RAG, which is still a win for trustworthiness. Calibration risk: N=16 paired golden runs is small; the bootstrap/sign-test CI will likely be wide, so the acceptance threshold must be stated as a CI bound (e.g. lower bound > 0) not a point estimate, and the report must say 'underpowered' if the CI straddles zero. Scoring risk: semantic-regression-check's axes (KB-ID coverage, anti-pattern recall) are proxies for plan quality, not human-acceptability — an on-arm that games KB-ID coverage without improving the real plan would false-positive; mitigate by reporting all three axes separately and spot-checking a few plans by hand. Token cost: 16x2 Stage-1 runs is real spend (~$0.07/plan per rag-phase1.md:99, so roughly $2-3 one-time) — acceptable for a one-time committed report. Provenance fix risk: extending loadCorpus to the 15 outputs/ docs may drop MAP@3 below the 0.6 gate if those plans are noisier; that is correct behavior (the gate should reflect production), but operators must be warned the number will move and the gate threshold may need re-discussion.
- **Indicator delta:** Today the rag indicator is effectively near-0% of the 96% bar: the indicator's definition is 'retrieval actually improves plans', and there is literally zero evidence of that (off by default, zero shadow/on runs, zero sidecars, the doc admits value is unmeasured, and the one metric measures ranking on the wrong 16-doc corpus dominated by framework self-matching). Landing workstream A (the offline ablation that produces a committed paired on-vs-off delta against the goldens with a CI) is the single biggest move: it converts the indicator from 'unmeasured' to 'measured', which is most of the distance — estimate +55 to +65 points (to ~60-70%) IF the ablation runs and reports, regardless of whether on beats off, because the indicator rewards an honest measurement of retrieval value. Workstreams B (MAP@3 provenance + KB-ID-only honest number, fixing the contradicted '31-doc' claim) and C (de-biased, CI-wired variance gate) add the remaining credibility — estimate +15 to +20 points, taking it to roughly 80-88%. Reaching the full 96% additionally requires the ablation to actually show on>off with a CI excluding zero (so the default can flip to on) and a re-run after the corpus grows beyond N=16 to tighten the CI; that last leg (~+8 to +15) is data-dependent and cannot be promised by code alone. Net: this fix moves rag from ~0% to a defensible ~80-88%, with a clear, data-gated path to 96%.

### grounding-cap-permanently-unreachable [trustworthy-measured-quality] · effort L
**Make the DOM probe follow imports into the POMs (and capture a Stage-1 snapshot) so the grounding cap can actually lift on real pwm-blueprint output**

- **Root cause:** The auto-ship gate `computeAggregateConfidence` caps confidence at UNVERIFIED_LOCATOR_CONFIDENCE_CAP=0.69 whenever `!r.domGrounded` (scripts/evaluate.ts:444,460). `domGrounded` is set by `domProbeConfirmed` (evaluate.ts:737, defined :685-694), which requires `totalLocators>0 && resolvedUnique>0 && notFound===0` in the `-dom-probe.json` report. That report is produced by scripts/dom-ground.ts, whose `extractLocators` (dom-ground.ts:169-205) reads ONLY the single `--probe` file via `project.createSourceFile(probePath,...)` and walks descendants of THAT file — it does not resolve imports. In migrate.yml:1162 the probe is fed only `outputs/tests/<base>/*.spec.ts`. But pwm-blueprint architecture puts ZERO locators in specs (verified: all 5 real specs return 0 getBy*/locator; every locator lives in POMs — cart.page.ts has 5, search-filter.block.ts has 12). So `extractLocators(spec)` returns [], `summarize` yields totalLocators:0, `domProbeConfirmed` returns false, and the 0.69 cap is a PERMANENT floor even with a live SUT. The asymmetry is the smoking gun: the credit-granting path `collectEmittedSources` (evaluate.ts:633-678, drives selectorQuality) DOES follow base.fixture + imports into the POMs and grants canonical-locator credit, while the verification probe sees an empty spec. cart.page.ts:21-29 ships getByTestId('cart-subtotal') and getByLabel(/quantity/i) under false 'CSS class locator kept as fallback' comments with NO fallback present — exactly the unverified guesses the cap was meant to catch, and they are invisible to the probe. Separately, plan.yml never captures a snapshot (no outputs/dom-snapshots dir; 0 plans carry dom-snapshot:role=; migrate.yml DOES capture at Stage 2 line 291-297 but generate.md has ZERO grounding refs, so the snapshot dies at the analyze->generate boundary). The README:497 '6/6 GREEN live calibration' is non-representative: all 6 tools/calibrate-pipeline/fixtures/dom-ground-live/*/probe.spec.ts have exactly 1 INLINE locator each — none mirror the locators-in-POM shape, so calibration validated the machinery against the wrong structure.
- **Recommended solution:** Two-part fix, both reusing machinery already in the repo. PART A (the real auto-ship unblock, ~90% of the value): make the probe see the SAME emitted tree the scorer credits. Refactor scripts/dom-ground.ts `extractLocators` to accept a SET of source files (the spec PLUS its imported POMs/blocks/fixtures) instead of one file, then probe the union. Reuse the existing import-following resolver: export `collectEmittedSources`-style file enumeration from evaluate.ts (today it returns a concatenated STRING; add/export a sibling `collectEmittedFiles(specPath): string[]` that returns the file LIST it already walks at evaluate.ts:644-676) and have dom-ground.ts import it, or pass the file list in via a new repeatable `--probe-extra` flag populated by migrate.yml. `liveProbeOne` (dom-ground.ts:256-278) already strips everything up to `.getBy`/`.locator` with the regex `replace(/^.*?\.(?=getBy|locator)/,'')`, so it probes `this.page.getByTestId('cart-subtotal')` identically to `page.getByTestId(...)` — no probe-driver change needed. This single change makes totalLocators>0 for real pwm-blueprint output, so a live SUT can resolve the POM locators, set notFound===0, and lift the cap legitimately — while cart.page.ts's bogus getByTestId('cart-subtotal') now surfaces as notFound and CORRECTLY keeps the cap on / routes to verify. PART B (close the plan->generate snapshot chain so grounding is preventive, not just punitive): (1) add a snapshot-capture step to plan.yml mirroring migrate.yml:291-297 (guarded by MIGRATION_TARGET_URL) so Stage 1 actually writes outputs/dom-snapshots/<base>.yaml that analyze.md:128-167 Step 4b already knows how to read; (2) add a grounding section to prompts/generate.md instructing Stage 2 to read outputs/dom-snapshots/<base>.yaml and the plan's // dom-snapshot:role= annotations, and to emit locators ONLY from snapshot-confirmed nodes (demote to locator(css)+LOW otherwise) — without this, the join key analyze.md:167 promises Stage 2 will consume is ignored. Then re-cut the calibration fixtures so at least 2 of the 6 dom-ground-live fixtures put their locator in a *.page.ts imported by probe.spec.ts, proving the import-following probe end-to-end and correcting the non-representative README:497 claim.
- **Concrete steps:**
  1. scripts/evaluate.ts: extract the file-enumeration logic currently inlined in collectEmittedSources (lines 644-676) into an exported `collectEmittedFiles(specPath: string, helperRootOverride?: string): string[]` that returns the absolute paths it walks (fixture-injected POMs via base.fixture, direct helper imports, transitive helper imports, and the <stem>.<layer>.ts union); refactor collectEmittedSources to call it and readFileSync+join, so there is ONE source of truth for 'what the emitted tree is'.
  1. scripts/dom-ground.ts: change `extractLocators(probePath: string)` to `extractLocators(probePaths: string[])` (or add a wrapper) that creates one ts-morph SourceFile per path and concatenates ProbedLocator[]; keep the existing descendant-walk and confidence-comment logic per file. Verify the comment-scope walk (lines 186-191) still finds the TODO/confidence comment above a `readonly x: Locator =` class-property initializer (PropertyDeclaration), not just Block statements.
  1. scripts/dom-ground.ts: add a repeatable `--probe-extra <file>` CLI option (parseArgs at lines 107+, multiple:true) OR import collectEmittedFiles directly; build the probe set = [spec, ...emittedPOMsAndBlocks]; update the header doc-comment (lines 20-26) to state the probe now covers the emitted tree.
  1. .github/workflows/migrate.yml:~1162: replace the spec-only probe loop so the probe is fed the spec AND its imported POMs/blocks. Simplest: invoke `npx tsx scripts/evaluate.ts`-style enumeration, or run dom-ground once with the spec and let it self-resolve imports via collectEmittedFiles. Keep the existing exit-code aggregation (DG_EXIT 0/1/2) and the same --report path so domProbeConfirmed still reads <base>-dom-probe.json.
  1. .github/workflows/plan.yml: add a snapshot-capture step copied from migrate.yml:285-297 — guard on `MIGRATION_TARGET_URL`, `mkdir -p outputs/dom-snapshots`, `npx tsx scripts/dom-snapshot.ts --url $MIGRATION_TARGET_URL --output outputs/dom-snapshots/<base>.yaml`, soft-fail to offline mode — so analyze.md Step 4b (lines 128-167) has a snapshot to read at plan time. This is the only point where capturing helps Stage 1.
  1. prompts/generate.md: add a '## DOM grounding (if snapshot present)' section that (a) tells Stage 2 to Read outputs/dom-snapshots/<base>.yaml, (b) consume the plan's `// dom-snapshot:role=...|name=...` annotations as the join key (per analyze.md:167), (c) emit getByRole/Label/Text ONLY for snapshot-confirmed nodes and demote everything else to locator(<css>) + LOW + a WHY comment. Mirror the abstention discipline already in analyze.md:153.
  1. tools/calibrate-pipeline/fixtures/dom-ground-live: convert at least 2 of the 6 fixtures so the probed locator lives in a `<name>.page.ts` imported by probe.spec.ts (locators-in-POM shape), proving the import-following probe resolves POM locators against a live SUT; update README.md:497 to describe the pwm-blueprint-shaped calibration instead of the inline-locator one.
  1. scripts/evaluate.ts: add a unit/calibration assertion that on a real pwm-blueprint output (spec with 0 inline locators + POM with N locators) the probe-fed file set has >0 locators, so this regression (totalLocators always 0) can never silently return.
- **Risk:** Part A widens what the probe tests, which can INCREASE notFound counts on real SUTs (cart.page.ts's bogus getByTestId('cart-subtotal') will now correctly fail) — this is the intended behavior, but it means more migrations route to verify before the prompts are tightened, so adopters may perceive a regression ('nothing ships') unless Part B (grounding-aware generation) lands close behind. Calibration needed: with the probe now covering 5-12 locators per POM instead of 0, the notFound===0 requirement in domProbeConfirmed (evaluate.ts:694) is far stricter; consider whether a single not-found should hard-block grounding or whether a threshold (e.g. resolvedUnique/total >= 0.9) better matches human-acceptable — but loosening must be measured, not assumed. ts-morph parsing of class-property-initializer locators must be verified (the comment-scope walk at dom-ground.ts:186-191 stops at Block|SourceFile; a PropertyDeclaration is neither a Block nor directly a statement, so confidence-comment attribution for HIGH-strict gating needs a test). The live probe runs each POM locator off page.getByX with no page-object navigation/auth context, so locators behind login or on a non-entry page will read not-found even when correct — the dom-snapshot.ts --flow DSL (lines 53-57) partially addresses snapshots but the dom-ground live probe has no equivalent multi-page/auth step, a real false-positive source. False-positive risk: requires a representative SUT; the current public-SUT calibration corpus does not mirror authenticated pwm-blueprint flows.
- **Indicator delta:** Large positive move, roughly +20-30 points toward 96% on trustworthy-measured-quality, but it is a PRECONDITION rather than a sufficient fix. Today the 'scorer is honest now' claim is structurally false: the grounding signal it rests on can never engage on real output (totalLocators always 0), so the scorer is either permanently pinned at 0.69 (nothing auto-ships, useless) or operators disable the cap and restore the documented anti-correlation (~0.69 conf / 60% auto-ship vs ~33% human-acceptable). Part A is the difference between a dead gate and a live one: it makes domGrounded ACHIEVABLE on the locators-in-POM shape that 100% of v0.2.0 output uses, so the cap can lift for genuinely DOM-confirmed migrations and stay down for the cart.page.ts-style unverified guesses it was designed to catch. That converts the grounding investment from decorative to load-bearing at the exact point the locator is written. It does NOT by itself prove the scorer now predicts human-acceptable (that still needs an n>=30 labeled batch with a rubric and CIs, and threshold calibration of the notFound===0 rule) — so it cannot alone reach 96%; it removes the structural blocker that currently caps this indicator far below 96 and makes the subsequent calibration measurable.

### ci-scores-wrong-spec [trustworthy-measured-quality] · effort M
**CI scores the WRONG spec**

- **Root cause:** test
- **Recommended solution:** test
- **Concrete steps:**
  1. Export the basename resolver from migrate-local
  1. Add a resolve CLI that exits nonzero on no match
  1. Add a resolve step before the AST diff gate
  1. Pass the scoped file to AST diff
  1. Use the resolved path in evaluate
  1. Re-run the report metric check after evaluate
  1. Fix the three reports and note in changelog
  1. Add a smoke guard on report output basename
- **Risk:** test
- **Indicator delta:** test

### cypress-selenium-never-reach-output [multi-framework] · effort L
**Run Cypress + Selenium end-to-end through Stage 2 and score them with the SAME honest harness as bad-PW, then gate promotion on measured acceptance**

- **Root cause:** The "multi-framework" claim rests on artifacts that look like coverage but never invoke Claude on Cypress/Selenium and never produce a scored Stage-2 output. Concretely: (1) `outputs/tests/` holds ONLY bad-PW specs; the only Selenium specs are in `outputs/tests/_legacy-v0.1.x/` which its own README marks archived/excluded (verified). (2) `outputs/plans/` DOES have full, high-quality Stage-1 plans + envelopes for Cypress (checkout-flow, wishlist) and Selenium (EmployeesTest, *JupiterTest, test_employees), with confidence-marked locators and "Source framework" declarations — but the pipeline is never carried into Stage 2. (3) `docs/measured-quality-baseline.md:24` measures only "Step 1, bad-Playwright, n=5". (4) `tools/calibrate-pipeline/run-calibration.ts` spawns ONLY deterministic validator scripts (kb-validate, plan-envelope-validate, ast-diff-trivial-check, validate-pwm-blueprint-conformance, etc.) — zero `anthropic`/`claude`/`messages.create`; its fixtures carry inline "// Anti-pattern #N … Conformance Check N must block" comments, i.e. reverse-engineered from the checks so 100% pass is guaranteed by construction. (5) `.github/workflows/regression-semantic.yml` runs Stage-1 (analyze) ONLY and compares plan-shape to `expected-plan.md` (line 196 'Run Claude (analyze stage)', line 250 'Compare emitted plan'). (6) `scripts/baseline-compare-selenium-java.ts` ALWAYS exits 0 (line 567), mixes shared `outputs/helper/page-object/pages/` POMs with no provenance link (self-documented lines 235-236), and compares only surface axes (anti-pattern counts, locator inventory, structural deltas) — explicitly NOT semantic equivalence. (7) `package.json` has only `baseline:selenium-java` — no Cypress baseline. The 70% acceptance promotion gate (docs/CONTINUE.md:85,121; docs/adr/0001-self-improvement-rag.md:424 'first-attempt SHIP IT rate ≥ 70%') therefore has literally zero LLM-measured datapoints for 3 of 4 frameworks. CRUCIALLY: the blocker is NOT missing infrastructure. The pieces all exist and are framework-agnostic: `scripts/migrate-local.ts` takes `--input <any path>` and runs the full validator wall; `scripts/ast-diff-trivial-check.ts` already parses `.java`/`.py` via tree-sitter-java/python; `scripts/evaluate.ts` scores any emitted PW tree by fixture-resolution (not by source framework); `inputs/{cypress,selenium-java,selenium-python}/` hold real inputs; and `examples/{cypress,selenium}-0N/expected-output.spec.ts` provide human-authored goldens for every case. The ONLY missing thing is: nobody ever pressed go and recorded the honest numbers, and there is no per-framework acceptance harness/rubric to record them into.
- **Recommended solution:** Build a per-framework MEASURED acceptance harness that mirrors the bad-PW path exactly, run it on the real Cypress + Selenium-Java + Selenium-Python corpora, and make framework promotion gate on the recorded numbers — reusing the framework-agnostic pieces that already exist instead of building anything new. Three coordinated changes: (A) A thin batch runner `scripts/measure-framework.ts --framework <cypress|selenium-java|selenium-python|bad-playwright>` that, per input: ensures a Stage-1 plan exists (derive-envelope), runs Stage 2 via the SAME code path as `migrate-local.ts` (same assembled generate prompt, same model, same validator wall), runs `scripts/evaluate.ts` to get the honest confidence, then computes a `golden-diff` acceptance signal against `examples/<framework>-0N/expected-output.spec.ts` (the human-authored reference) using the metrics evaluate already extracts (selector-quality, web-first-assertion-rate, smell-delta, forbidden-pattern set) PLUS the cross-language `ast-diff-trivial-check` to prove non-triviality. It writes one row per migration to `outputs/.metrics.db` tagged with a `framework` column and emits `docs/measured-quality-<framework>.md` in the exact table shape of `docs/measured-quality-baseline.md`. (B) A small binary acceptance RUBRIC doc `docs/acceptance-rubric.md` (reason-coded: hallucinated-locator / dropped-assertion / brittle-fallback-CSS / try-catch-in-POM / semantic-drift-from-source / clean-SHIP) so "acceptable" stops being a vibe — the same rubric the prior multi-agent review implicitly used, written down once and applied uniformly across all four frameworks. (C) A promotion gate: extend `regression-semantic.yml` (which already has a `framework_filter` choice input incl. cypress/selenium) with a Stage-2 measurement job that fails the run if a framework's measured first-attempt auto-ship rate is reported as "promoted" without ≥ the bad-PW bar, and replace the always-exit-0 `baseline-compare-selenium-java.ts` verdict with the measured acceptance number. Net effect: the 70% gate stops being a claim with zero datapoints and becomes a number you can read per framework — and the honest number (likely low, like bad-PW's 33%) is surfaced rather than hidden behind hand-authored fixtures.
- **Alternatives considered:** (1) Just promote based on the static examples/<fw>/expected-output.spec.ts goldens — REJECTED: those are human-authored references that no LLM is ever scored against (validate-examples.ts only cross-references KB IDs in the plan; regression-test.yml's examples-contract only asserts the files EXIST), so promoting on them measures nothing about the actual pipeline. (2) Build a brand-new cross-framework eval framework — REJECTED as wasteful and drift-prone: evaluate.ts, the validator wall in migrate-local.ts, and ast-diff-trivial-check.ts are ALREADY framework-agnostic (ast-diff parses .java/.py via tree-sitter; evaluate scores by fixture-resolution not by source framework). Reusing them keeps the measured path identical to production; a parallel harness would silently diverge and produce numbers that don't reflect real runs. (3) Fix baseline-compare-selenium-java.ts in place (make it exit non-zero, add Cypress) — PARTIALLY ADOPTED but insufficient alone: its core flaws are no per-provenance link (mixed POM pool) and surface-only axes; the cleaner move is a provenance-aware measure-framework.ts that supersedes it and covers all four frameworks uniformly. (4) Add semantic-equivalence checking as the acceptance signal — DEFERRED to the H4 work-item: full source→output assertion-intent equivalence is its own severity-5 problem; here the golden-diff + rubric review is the pragmatic acceptance screen, and the rubric explicitly carries a semantic-drift reason code so the gap is named, not hidden.
- **Concrete steps:**
  1. Write /Users/kapusansky/DEV/PWmodernizer/docs/acceptance-rubric.md: a binary SHIP/FIX-FIRST/REJECT rubric with reason codes (hallucinated-locator, dropped-assertion, brittle-fallback-CSS, try-catch-in-POM, semantic-drift-from-source, assertion-roulette). Source the reason codes from the existing labeled run in docs/measured-quality-baseline.md (the n=6 'Run 2' table at lines 50-75 already enumerates FIX-FIRST defects) so the rubric is grounded in the one real human-equivalent review that exists, not invented.
  1. Create /Users/kapusansky/DEV/PWmodernizer/scripts/measure-framework.ts. Import and reuse the existing exports from migrate-local.ts (derivePaths, the validator-wall step list) and evaluate.ts (collectEmittedSources, the metric extractors) rather than re-implementing. CLI: --framework <name> [--mock] [--report-out docs/measured-quality-<framework>.md]. For each input under inputs/<framework>/ (skip _provenance, _stress): ensure plan exists (outputs/plans/<base>.md) else run Stage 1 or skip with a clear 'plan missing' row; run Stage 2 via the migrate-local code path; run the validator wall; run evaluate.ts; compute golden-diff vs examples/<framework>-0N/expected-output.spec.ts. Emit a baseline-shaped table + write rows to outputs/.metrics.db with a new framework column.
  1. Add a `framework` TEXT column to the metrics schema (find the CREATE TABLE / migration in scripts/metrics.ts — MetricsDB) so per-framework first-attempt auto-ship rate is queryable by metrics:report. Default existing rows to 'bad-playwright' in the migration so the n=5 baseline is retro-tagged, not lost.
  1. Add a golden-diff helper inside measure-framework.ts that reuses evaluate.ts's selector-quality + web-first-assertion-rate + smell-delta + forbidden-pattern extractors on BOTH the emitted spec tree AND examples/<framework>-0N/expected-output.spec.ts, and reports per-axis deltas. This is the cheap, deterministic 'did Sonnet match the human golden on the axes we score' signal — the same idea baseline-compare-selenium-java.ts has, but (a) per-provenance (one golden per input, not a mixed pool) and (b) for ALL frameworks incl. Cypress which has none today.
  1. Add package.json scripts: `measure:cypress`, `measure:selenium-java`, `measure:selenium-python`, `measure:bad-playwright` (the last to prove the harness reproduces the known n=5 baseline — a self-check that the new harness is honest), plus `measure:all`. Replace the always-exit-0 baseline-compare-selenium-java.ts call sites with measure-framework where used; keep baseline-compare as a deprecated thin wrapper or delete it.
  1. Extend .github/workflows/regression-semantic.yml: it already has the framework_filter choice input (all/bad-playwright/selenium-java/selenium-python/cypress) and the token-routing for Stage 1. Add a workflow_dispatch-gated Stage-2 measurement job that runs measure-framework.ts for the selected framework, uploads docs/measured-quality-<framework>.md as an artifact, and FAILS the run if a docs/PROMOTION-STATUS.md marks a framework 'promoted' whose recorded auto-ship rate is below the bad-PW bar. This converts the 70% gate from a doc claim into a CI-enforced, evidence-backed gate.
  1. Write docs/PROMOTION-STATUS.md: a single table (framework | corpus n | first-attempt auto-ship % | human-acceptable % | status: example|measuring|promoted) seeded from the measure:all run. Be explicit and honest: today every framework except bad-PW is 'example' with n=0 measured; the goal is to move them to 'measuring' with real n and only to 'promoted' when measured ≥ the bad-PW bar. This is the artifact that makes the multi-framework claim auditable.
  1. Run `npm run measure:cypress` and `npm run measure:selenium-python` end-to-end (real tokens) on at least the 2 Cypress + 2 selenium-python single-file inputs to seed the FIRST real datapoints, commit the resulting docs/measured-quality-*.md, and record the honest numbers in PROMOTION-STATUS.md — even if (especially if) they are low.
- **Risk:** Three calibration risks. (1) FALSE PROMOTION: golden-diff is a surface-axis proxy, not semantic equivalence (the goldens are different programs than nothing — they ARE the human reference for the same input, so this is better than baseline-compare's cross-program comparison, but matching selector-quality to a golden does NOT prove the test asserts the right thing). Mitigation: the rubric's semantic-drift-from-source reason code must be applied by a human/agent review pass on the first batch, exactly as the n=6 bad-PW Run-2 review did — the deterministic golden-diff is a SCREEN, the rubric verdict is the ACCEPTANCE. Do not let golden-diff alone flip 'promoted'. (2) SMALL-n THEATER: Cypress has only 2 inputs, selenium-python 2 single-file + 1 multifile. Reporting '100% auto-ship (n=2)' would repeat exactly the n=5 over-claim the prior session warned against (docs/measured-quality-baseline.md). Mitigation: PROMOTION-STATUS.md must show raw n and a CI/Wilson interval, and the gate must require a minimum n (e.g. ≥5 per framework) before 'promoted' is even selectable — which means expanding the Cypress/selenium corpora is a prerequisite, surfaced as a TODO. (3) COST: real Stage-2 runs burn tokens (docs/CONTINUE.md: ~1 Pro/Max daily quota for a heavy batch). Mitigation: --mock wiring-check first, sequential not parallel (concurrency group cancels parallels — CONTINUE.md), and gate the CI Stage-2 job behind workflow_dispatch only, never per-PR. The harness reusing migrate-local.ts's exact code path is what keeps it honest — if it drifts from the production path the numbers are worthless, so step 2 must IMPORT migrate-local's wall, not copy it.
- **Indicator delta:** Large, because this indicator is currently near floor on honest grounds: 3 of 4 frameworks have ZERO measured Stage-2 datapoints, so a truthful score is ~25-40% (one framework measured, the rest example-status props that 100%-pass-by-construction). Landing steps 1-5 (the harness + rubric + first real Cypress/selenium-python batch) is the single biggest honest mover: it converts 'example status' into 'measured status' for 3 frameworks, which is the literal definition of the indicator. Estimate +35-45 points, to ~70-80%, on first measured batch — NOT to 96%, and deliberately so: the remaining gap to 96% is (a) corpus size (need ≥5-10 real inputs per framework with CIs, addressed by the n-floor in the gate but requiring more inputs than exist today) and (b) the honest acceptance number will likely be low (bad-PW is 33% human-acceptable), so 'promoted' status won't be earned until Stage-2 generation quality improves (the H8 prompt-tuning / generation-quality work). This fix does not fake that — it exposes it, which is exactly what 'trustworthy multi-framework readiness' requires. Reaching 96% on this indicator needs this harness PLUS expanded corpora PLUS the generation-quality lift to clear the bar on the measured numbers; this PR delivers the measurement spine that makes the other two auditable.

### plan-confidence-keyword-scan [trustworthy-measured-quality] · effort M
**Plan confidence (heaviest 0.40 weight) is a gameable prose keyword scan; replace it by reading the already-validated structured confidence enum from the plan envelope**

- **Root cause:** planConfidence (scripts/evaluate.ts:335-356) classifies EVERY markdown line matching /^\|/ via /\bhigh\b/ then /\bmed\b/ then /\blow\b/ first-match-wins, matching the keyword ANYWHERE in the line — prose, KB rationale, Evidence cells, severity columns — not a confidence column. It feeds the single heaviest input (aggregate * 0.4, evaluate.ts:454, report weight 0.40 at :491). Verified divergence on real plans: force-clicks envelope locatorTable=[high,high,high,high,low,high,high,high]; flaky-waits=[med,med,med,low,med]; FluentWait=[high]. The honest structured aggregate for flaky-waits is (4*0.6+1*0.2)/5 = 0.52, but the prose scan misreads its TWO tables (a warn/info SEVERITY column and a real Confidence column) and the Evidence prose '(HIGH confidence...)', inflating it. CRITICAL ENABLER for the fix: the structured truth already exists and is already trusted. The envelope JSON sidecar carries locatorTable[].confidence as a schema-enforced enum ['high','med','low'] (plan-envelope.schema.json:166-170), and migrate.yml validates that envelope as the FIRST fail-fast gate before any token spend (lines 209-237), deriving it from markdown via derive-envelope.ts when absent. So when evaluate.ts runs, a strict-validated, ungameable confidence column is sitting on disk next to the plan — the scorer simply ignores it and scrapes prose instead. Compounding: planConfidence is not exported and has ZERO unit tests (evaluate.test.ts only hand-sets confidence on Report objects; the one plan-markdown fixture at :204 is a trivial single-row table), so the misread has never been caught.
- **Recommended solution:** Stop scraping prose; read the structured, already-validated confidence enum. Change planConfidence to source confidences from the plan envelope's locatorTable[].confidence (the schema-validated machine contract), and only fall back to a COLUMN-AWARE markdown parse — never the keyword scan. Concretely: (1) Resolve the envelope path from the plan path (`<plan>`.replace(/\.md$/,'.envelope.json'), mirroring the `<basename>.envelope.json` convention migrate.yml uses). (2) If the envelope exists, parse it and aggregate over locatorTable[].confidence using the existing High=1.0/Med=0.6/Low=0.2 weights — this is ungameable because the enum is schema-restricted and prose 'high' in a notes cell cannot become a 'high' row. (3) If no envelope, reuse the EXPORTED column-aware parser already in the repo — derive-envelope.ts:parseLocatorTable (which finds the H/M/L *cell* via /^(high|med|medium|low|h|m|l)$/i, not a substring) — instead of the line scan. (4) Keep the total===0 → 0.5 neutral default. (5) Export planConfidence and add unit tests that lock in the three real divergence cases (force-clicks, flaky-waits severity-vs-confidence two-table trap, FluentWait prose 'HIGH-confidence fallback'). This is a from-the-source measurement of the reviewer's own stated per-locator confidence, not a writable-by-writing-'high'-more-often prose statistic. It also tightens the H1 anti-correlation story: combined with the existing UNVERIFIED_LOCATOR_CONFIDENCE_CAP=0.69 (evaluate.ts:444) that already routes ungrounded migrations to verify, the 0.40 weight now reflects genuine low/med locator confidence and will pull more genuinely-uncertain plans under the 0.7 verify gate instead of letting prose inflate them over it.
- **Concrete steps:**
  1. scripts/evaluate.ts: add optional `envelope?: string` to interface Args (after :59) and to parseCliArgs options (:62-77), so the caller MAY pass an explicit envelope path; do not make it required.
  1. scripts/evaluate.ts: import { parseLocatorTable, normaliseConfidence } from './derive-envelope' (these are already exported at derive-envelope.ts:163,181) plus extractSection if needed for the markdown-fallback body.
  1. scripts/evaluate.ts: rewrite planConfidence (335-356) into an EXPORTED function `export function planConfidence(planMd: string, envelopePath?: string): PlanConfidence`. Order of precedence: (a) if envelopePath given and exists, JSON.parse it and read locatorTable[].confidence; (b) else if the conventional sibling `<plan>.envelope.json` exists, use it; (c) else parse the markdown via parseLocatorTable on the '## Locator translation table' section. In all cases tally high/med/low from the enum values only, never a substring scan.
  1. scripts/evaluate.ts: keep the weighting aggregate = (high*1 + med*0.6 + low*0.2)/total and the total===0 -> {aggregate:0.5} neutral default exactly as today, so only the SOURCE of high/med/low changes.
  1. scripts/evaluate.ts main() (:733): pass the resolved envelope path — derive it from args.plan (`args.plan.replace(/\.md$/i,'.envelope.json')`) or args.envelope when provided — into planConfidence.
  1. scripts/evaluate.test.ts: add a describe('planConfidence') block. Fixtures: (1) an envelope JSON with locatorTable=[high*7,low] asserts aggregate 0.90; (2) flaky-waits-style envelope [med*4,low] asserts aggregate 0.52 AND a markdown-only variant whose prose says '(HIGH confidence...)' in a notes cell + a 'warn/info' severity table asserts the SAME 0.52 (proves prose can't inflate); (3) FluentWait markdown with a 'HIGH-confidence fallback' prose line but a single Mechanical 'high' confidence cell asserts 1.0 via the column path, and that the stray prose line is not double-counted; (4) a plan with a non-high/med/low vocabulary asserts the 0.5 neutral default.
  1. Add the new test script to the smoke chain if planConfidence gets its own file, or extend the existing test:evaluate target (package.json:58 already runs test:evaluate in smoke).
  1. Optional hardening: in migrate.yml's evaluate step, pass `--envelope outputs/plans/${BASE}.envelope.json` explicitly so the scorer binds to the SAME strict-validated envelope the FIRST gate (migrate.yml:209-237) already checked, removing any path-derivation ambiguity.
- **Risk:** Low-to-moderate. (1) The envelope's locatorTable confidence is the REVIEWER's pre-DOM confidence in each locator translation, not a holistic plan confidence — but that is already exactly what today's scan approximates (it counts locator-table rows), so this is a strictly more faithful read of the same intent, not a semantic change. (2) Coverage shift: derived envelopes (safety-net path) may have fewer/cleaner rows than the markdown the old scan walked, so some plans' aggregate will MOVE on re-score; this is correct (the old numbers were misreads) but will perturb the tiny n=5/6 baselines in docs/measured-quality-baseline.md — re-run npm run calibrate and re-baseline rather than treating drift as regression. (3) parseLocatorTable returns 'med' for an unidentifiable confidence cell (derive-envelope.ts:199) — slightly generous, but bounded to the enum and far safer than the prose scan; document it. (4) Residual gaming surface shrinks but is non-zero: a Stage-1 model can still over-claim 'high' in the structured cell — however that now requires lying in a schema-validated, reviewer-facing column that a human approves at the Stage-1 PR gate, which is the intended human-in-the-loop control, not a hidden prose side-channel. (5) Must keep the 0.69 cap intact; this fix complements it, does not replace it. False-positive risk on the gate itself is near zero because the change only re-sources an existing weight.
- **Indicator delta:** Estimate +6 to +9 points toward 96% on trustworthy-measured-quality. Rationale: this neutralizes the single largest gameable input (0.40 weight) on the scorer, converting it from a writable-by-prose statistic into a from-source read of a schema-validated enum, plus adds the first real unit tests for the function (it currently has zero). That directly attacks H1 (scorer anti-correlation): the 40% weight stops rewarding 'write high more often' and starts reflecting genuine low/med locator uncertainty, which — together with the existing 0.69 ungrounded cap — routes more genuinely-uncertain plans under the 0.7 verify gate. It is NOT a full close on H1: semantic equivalence to the source (H4) and the tiny-n / no-rubric problem (H2) remain the dominant unresolved blockers, and the locator-table confidence is still a proxy for whole-plan correctness. So this is a large, evidence-backed slice of the trust gap, not the whole gap; the indicator cannot reach 96% until H2/H4 are also addressed.

### validator-runs-over-whole-tree [pipeline-engineering] · effort M
**Scope the Stage-2 validator wall (tsc/eslint/playwright --list) to the current leg's files instead of the whole accumulated outputs/ tree**

- **Root cause:** The Stage-2 "validator wall" type-checks, lints, and parse-lists the ENTIRE accumulated output tree, not the input being migrated. migrate.yml:759 runs `npx tsc --noEmit -p outputs/tests/tsconfig.json`; that tsconfig extends outputs/tsconfig.json whose `include` is `tests/**/*.ts` + `helper/**/*.ts` (verified by reading both files), so one invocation type-checks every accumulated sibling spec AND every shared helper. eslint at :770 globs `outputs/tests/**/*.ts`; playwright at :782-792 does `find outputs/tests -name '*.spec.ts'; playwright test --list "${SPECS[@]}"` over ALL specs; the re-validate retry repeats the same whole-tree commands at :908-917. The tree genuinely accumulates (9 sibling specs on disk; migrate.yml:1010 comment 'outputs/tests/ accumulates legacy v0.1.x specs'). The real coupling point is the SHARED outputs/helper/fixtures/base.fixture.ts: it imports PageClassCart/Dashboard/Login/ProductListing/SearchFilters (one shared fixture importing every migration's POM, verified by reading the file), every spec imports `test` from it, and generate.md instructs each leg to EXTEND that same file (migrate.yml:431-434, :873). Under the fail-fast=false parallel matrix (migrate.yml:29-31), concurrent legs mutate the same base.fixture.ts, so a whole-tree tsc sees everyone's half-edits. Net: the wall verdict is a property of the whole repo and depends on run order — a type error in a sibling POM or a mid-edit shared fixture reds the current leg though its own files are clean (false negative -> verify.yml:510-535 START OVER -> wasted Sonnet+Opus tokens); the inverse can mask a real defect. The author already scoped the OTHER validators by basename (validate-pwm-blueprint-conformance --input-basename at :1010-1012, plan-envelope-validate, ast-diff dir-mode) but deliberately left tsc/eslint/playwright whole-tree — a confirmed inconsistency. The 403->10x cost rider in the original framing is a HISTORICAL bug already fixed: `actions: write` is present in both migrate.yml:84 and verify.yml:76 (PR #127), so it is a regression-risk guard, not a live amplifier; severity rests on the determinism defect + token waste.
- **Recommended solution:** Make the wall verdict a function of THIS leg's files only, while still type-checking against the real shared helpers (you cannot compile the spec truly standalone because it imports @fixtures/base.fixture which imports every POM). Two-part fix.

PART A (primary — scope the verdict by ownership). Determine the leg's own files from the uncommitted working tree: the PR/commit is opened only at migrate.yml:1271, AFTER the whole wall, so during validation the leg's generated files are exactly `git status --porcelain` under outputs/ minus _legacy. Compute LEG_FILES once after the generate step. Then: (1) tsc — keep one `tsc --noEmit -p outputs/tests/tsconfig.json` so cross-file resolution against shared helpers still works, but pipe its output through a filter that keeps only diagnostics whose file path is in LEG_FILES; STATUS=1 only if a LEG_FILES diagnostic remains. This preserves 'a broken shared import THIS leg introduced still fails' while immunizing the leg from a pre-existing sibling/half-merged-fixture error it didn't cause. (2) eslint — replace the `outputs/tests/**/*.ts` glob with `eslint --fix "${LEG_FILES[@]}"` (eslint is already per-file). (3) playwright --list — replace `find outputs/tests -name '*.spec.ts'` with the spec(s) in LEG_FILES only. Keep a WARN-only whole-tree tsc pass that emits ::warning:: for non-owned errors so cross-file regressions stay visible without blocking.

PART B (eliminate the parallel-mutation source). The fail-fast=false matrix lets concurrent legs write the same shared base.fixture.ts. Run each leg's wall inside an isolated workdir — copy outputs/ into $RUNNER_TEMP/leg-<basename>/ before validate so each leg's tsc sees a private snapshot plus only its own edits — or serialize writes to shared helper files via a per-shared-file concurrency lane. Ship A first (small, high-leverage), then B for true determinism under batch.
- **Alternatives considered:** (1) Truly isolated single-spec compile (copy only the spec + stub fixture): rejected — the spec imports @fixtures/base.fixture which transitively imports every POM, so a standalone compile would fail to resolve or require synthesized stubs, losing the real cross-file type safety that catches genuine integration bugs. (2) Per-leg tsconfig with files:[thisSpec]: rejected as brittle — it still pulls the whole helper graph (hence siblings via base.fixture) so it does not actually scope the verdict; the post-filter scopes the VERDICT while keeping full-graph resolution. (3) Just exclude _legacy: insufficient — contamination is from ACTIVE siblings and the shared base.fixture.ts, not only the already-excluded _legacy dir. (4) Make the wall warn-only: rejected — trades a false-negative for a false-positive (masks real defects), worsening trustworthy-measured-quality. The chosen git-porcelain ownership + tsc post-filter fits THIS codebase because the commit happens after the wall (migrate.yml:1271), so the working tree already encodes leg ownership with zero new bookkeeping, and it mirrors the author's own established --input-basename scoping pattern.
- **Concrete steps:**
  1. Add a 'Compute this leg's files' step in .github/workflows/migrate.yml right after the generate/usage steps and before the 'Validate generated code' step (~line 747): run `git -c core.quotepath=false status --porcelain -- outputs/ | awk '{print $2}' | grep -v '/_legacy-v0.1.x/'` into LEG_FILES.txt. These are precisely this leg's emitted/extended files since the PR commit happens later at migrate.yml:1271.
  1. In 'Validate generated code' (migrate.yml:756-792): keep `npx tsc --noEmit -p outputs/tests/tsconfig.json 2>&1` (cross-file graph against shared helpers is required for correctness) but post-filter — after capturing TSC_OUT, set TSC_RC=1 only if any line of TSC_OUT begins with a path present in LEG_FILES.txt; emit non-owned tsc errors as ::warning::. Write a small `scripts/filter-tsc-to-leg.ts` (tsx) taking tsc output + LEG_FILES.txt, emitting only owned diagnostics + the right exit code, so the bash stays simple and the logic is unit-testable.
  1. Replace eslint line :770 `npx eslint --fix 'outputs/tests/**/*.ts'` with eslint over the LEG_FILES.txt entries ending in .ts (guard empty list -> skip, RC=0).
  1. Replace the playwright block :782-792: build SPECS from LEG_FILES.txt filtered to `*.spec.ts`; if empty, PW_RC=0. Apply the identical three changes to 'Re-validate after fix retry' at migrate.yml:908-917 so the retry leg is scoped the same way.
  1. Add scripts/filter-tsc-to-leg.test.ts (matching the repo's existing script-test convention referenced in the conformance validator header) with cases: (a) sibling-POM error not in LEG_FILES -> exit 0; (b) error in a LEG_FILES spec -> exit 1; (c) error in base.fixture.ts when base.fixture.ts IS in LEG_FILES (leg extended it) -> exit 1.
  1. Calibrate: run the scoped wall against the current 9-spec tree and confirm zero owned-error specs flip verdict vs the whole-tree wall (regression baseline).
  1. PART B (follow-up step): before validate, copy outputs/ into $RUNNER_TEMP/leg-${BASE} and run tsc/eslint/playwright there, OR add concurrency serialization on shared-helper writes. Document in docs/AUDIT-96-PREP.md that Part A scopes the verdict and Part B removes the parallel-write race.
  1. Update the comments at migrate.yml:782-792 and the 'accumulates legacy v0.1.x' note at :1010 to state the wall is now leg-scoped, mirroring the existing --input-basename rationale at :1004-1012 so the inconsistency the author left is closed and documented.
- **Risk:** FALSE-NEGATIVE INVERSION: post-filtering tsc to owned files could mask a genuine break THIS leg causes in a sibling (e.g., leg edits base.fixture.ts in a way that breaks sibling specs but the error surfaces on the sibling's line). Mitigation: when the leg edits base.fixture.ts that file IS in LEG_FILES, so self-introduced regressions usually also annotate the fixture or the leg's own spec; for the residual cross-file case keep the WARN-only whole-tree tsc pass (::warning::) so visibility survives without a false-negative block. PART-B RACE persists until shipped: two legs extending base.fixture.ts concurrently can still interleave writes — Part A makes each leg's VERDICT independent but not the FILE; flag explicitly that Part A alone does not fix lost-update on base.fixture.ts content (a separate correctness issue), only pass/fail determinism. Low risk `git status --porcelain` mis-identifies ownership: verify base.fixture.ts edits show as modified (they do, it is checked into main). Calibrate against the 9-spec tree before merge to ensure no real owned defect is newly hidden.
- **Indicator delta:** +4 to +6 points toward 96% on pipeline-engineering (robustness/determinism/failure-handling). This is the run-order/shared-state non-determinism defect that most directly converts a CLEAN migration into a START-OVER (verify.yml:510-535) plus wasted Sonnet+Opus tokens, and it sits on the single most expensive gate (the wall preceding the Opus dual-lens). Removing order-dependence there materially raises determinism. It does not by itself close to 96% because Part B (the parallel base.fixture.ts write race) and the broader semantic-equivalence gap (H4) remain, and the historical 403->regen amplifier is already fixed so there is no extra cost-determinism win to bank. Bounded as a determinism/false-negative fix on one gate, not a coverage expansion: meaningful, not transformational.

### no-local-stage1-for-own-test [adoptability] · effort M
**No local way to plan your OWN test: add `npm run plan` (local Stage-1) so the clone-and-run loop closes end-to-end**

- **Root cause:** Local Stage 1 does not exist for an arbitrary input; only the demo-wired try-it.ts can plan, and Stage-2 migrate-local.ts hard-errors when the plan file is absent — so the documented local route forces CI for the planning step.
- **Recommended solution:** Add a dedicated local Stage-1 command, `npm run plan -- --input <path>`, that ports plan.yml's Claude invocation + plan-structure validation + envelope derivation to the developer's machine, producing outputs/plans/<base>.md (+ .envelope.json) for ANY input. This closes the clone-and-run loop: `npm run plan -- --input X` then `npm run migrate -- --input X`. Keep try-it.ts as the zero-config demo onboarding wrapper (do NOT overload it with --input — it is intentionally a fixed 90-second tour). Refactor the shared real-Claude logic (auth detection, prompt build mirroring plan.yml:716-762, spawnSync of @anthropic-ai/claude-code, adaptive max-turns, structure validation, derive-envelope fallback) into a small module both try-it.ts and the new plan-local.ts import, so the two never drift. Then fix migrate-local.ts to (1) make `ready` in --check require the plan to exist when --input is given AND surface the new command in the hint, (2) change the "no approved plan" dead-end (:605) to point at `npm run plan -- --input <p>` (the local fix) instead of only CI, and (3) disclose the ~30 MB claude-code download + acceptEdits agentic behavior in quickstart Prerequisites. Reuse, do not reinvent: derive-envelope.ts, plan-envelope-validate.ts, and validate-plan-dom-grounding.ts already exist as standalone CLIs (migrate-local.ts:203-244 already shells them); the new command just runs the same offline-abstention gate post-plan for parity with plan.yml:880-892.
- **Alternatives considered:** Alt A: Overload try-it.ts with --input. Rejected — try-it is intentionally a fixed 90s onboarding tour (try-it.ts:2-26 header) with a hard-coded max-turns 12 known to fail real inputs (plan.yml:621); generalizing it muddies the onboarding contract and re-imports the max-turns bug. Alt B: Make migrate-local.ts auto-plan when the plan is missing (call Claude for Stage 1 inside Stage 2). Rejected — it collapses the deliberate human-review-after-plan gate (CLAUDE.md: 'PR-based human approval after Stage 1') into one silent token-spend, and breaks the envelope-is-trusted-if-present contract (migrate-local.ts:223). A separate `npm run plan` keeps the plan reviewable before generate. Alt C: Doc-only fix (tell users to use CI for planning). Rejected — leaves the core 'clone and run locally without wiring Actions secrets' promise (migrate-local.ts:3-8) unmet and the misleading 'Ready' doctor in place. The chosen approach (dedicated plan-local.ts mirroring plan.yml, shared prompt module, doctor + message fixes) is the only one that makes the local loop real while preserving the review gate.
- **Concrete steps:**
  1. 1. Create /Users/kapusansky/DEV/PWmodernizer/scripts/plan-local.ts (new file). parseArgs supports --input <path> | --inputs <glob> | --mock | --check | --help (reuse the expandInputs() glob walker from migrate-local.ts:557-571 — export it if not already, it is). Derive OUTPUT_PLAN = outputs/plans/<basename(input)>.md and ENV = outputs/plans/<basename>.envelope.json, mirroring migrate-local.ts derivePaths (:99-112) so Stage 2 finds the plan at the path it expects.
  1. 2. In plan-local.ts, mirror plan.yml's Claude call verbatim: build the analyze.md wrapper prompt (copy the heredoc text at .github/workflows/plan.yml:716-762, substituting INPUT_PATH/OUTPUT_PATH), and spawnSync('npx', ['--yes','@anthropic-ai/claude-code','--model','claude-sonnet-4-6','--max-turns',<adaptive>,'--print','--permission-mode','acceptEdits', prompt], {stdio:['ignore','inherit','inherit']}). Port the adaptive max-turns complexity score from plan.yml:611-672 (or start with a fixed 20 and TODO the adaptive tier) — do NOT use try-it's hard-coded 12, which already caused error_max_turns on real inputs (see plan.yml:621 comment).
  1. 3. After the Claude call, port plan.yml's three post-plan gates as spawnSync calls: (a) plan-structure validation — assert the 8 '##' sections + a HIGH/MED/LOW marker exist (plan.yml:829-852); (b) envelope: if ENV absent, run `scripts/derive-envelope.ts --plan <PLAN> --out <ENV>` then `scripts/plan-envelope-validate.ts --envelope <ENV>` (plan.yml:910-928); (c) the offline abstention gate `scripts/validate-plan-dom-grounding.ts --plan <PLAN> --source <INPUT>` (plan.yml:890) so a hallucinated local plan is caught exactly as in CI. Print a clear pass/fail summary like migrate-local's reportOutcome.
  1. 4. Refactor shared logic to prevent drift: extract detectAuth + the auth-env wiring (try-it.ts:73-79,158-165 ≡ migrate-local.ts:114-120,350-352) and a single buildStage1Prompt() into scripts/stage1-shared.ts (new), and have BOTH try-it.ts and plan-local.ts import it. try-it.ts then becomes buildStage1Prompt(SAMPLE_INPUT, OUTPUT_PLAN); plan-local.ts calls it with the user's paths. This is the pwm-blueprint 'reuse, don't duplicate' rule.
  1. 5. Wire package.json: add "plan": "tsx scripts/plan-local.ts" next to "migrate" (package.json line ~ after the "migrate" entry). Add a smoke test scripts/plan-local.test.ts mirroring migrate-local.test.ts: assert path derivation matches plan.yml's BASE and that --mock/--check do not call Claude (export the pure functions, gate main() behind the import.meta.url check exactly as migrate-local.ts:699-701).
  1. 6. Fix migrate-local.ts --check doctor (:136-145): when args.input is set, fold the plan-existence check into the verdict and point the hint at the new command — change line 139's hint to 'run npm run plan -- --input <p> (local) or plan.yml (CI)', and change `const ready = nodeOk && authOk;` (:141) to also require plan presence when --input is supplied, so it can no longer print 'Ready' while showing '✗ plan'.
  1. 7. Fix the Stage-2 dead-end message in migrate-local.ts:605: change 'Run Stage 1 first (plan.yml, or `npm run try-it` for the demo).' to 'Run Stage 1 first: `npm run plan -- --input <p>` (local) or plan.yml (CI). `npm run try-it` only plans the bundled demo.' Update the same stale guidance in printHelp (:92-93) and the file header (:13-14).
  1. 8. Update docs/quickstart.md: in §1 Prerequisites disclose that the real Claude call shells out to `npx @anthropic-ai/claude-code` (downloads ~30 MB on first run, runs agentically with --permission-mode acceptEdits). In §4.4 add the missing local planning step before the migrate line: `npm run plan -- --input inputs/<framework>/your-bad-test.spec.ts` then the existing `npm run migrate -- --input ...`, so the local route is genuinely end-to-end.
  1. 9. Verify: run `npm run smoke` + the new test; then `npm run plan -- --input examples/... --mock`-style wiring check (add a --mock to plan-local that copies the demo mock plan or skips the Claude call) to prove the loop without spending tokens.
- **Risk:** Drift between local plan-local.ts and plan.yml is the main risk: the adaptive max-turns table, the analyze.md prompt text, and the gate set all live in YAML and could diverge after this lands (the same parity burden migrate-local.ts already carries vs migrate.yml). Mitigate by extracting the prompt/auth into stage1-shared.ts (step 4) and adding a parity test, but the YAML side stays a copy — flag it with a header comment pointing at plan.yml line ranges, as migrate-local.ts does. Secondary risk: plan-local runs Claude with --permission-mode acceptEdits locally (writes files into the user's clone) — acceptable since migrate already does this, but the token-spend + filesystem-write warning must print BEFORE the call (mirror migrate-local.ts:348). Low false-positive risk on the doctor change — tightening `ready` only makes it stricter, never looser. No risk to CI (this is additive; plan.yml is untouched). The offline-abstention gate (step 3c) could surface as a confusing local failure on a hallucinated plan, but that is correct behavior and matches CI.
- **Indicator delta:** Large move on adoptability — this removes the single hardest blocker to "a new user can run it and trust the output." Today the documented local route (quickstart §4.4) is broken for any real input: the user hits 'no approved plan' with no local way forward, and the doctor lies 'Ready'. After the fix there is a genuine clone-and-run loop (plan -> migrate -> validator wall) with no GitHub Actions secrets, plus an honest doctor and disclosed dependencies. Estimate roughly +20-25 points toward 96% on adoptability (e.g. from a ~70s to low-90s band): it converts the primary first-run dead-end into a working path and fixes the misleading 'Ready' verdict and the undisclosed 30 MB agentic download — the three concrete friction points a new adopter actually hits in the first five minutes. Remaining gap to 96% is unaffected first-run friction (Node-22 hard requirement, token validity/network still unchecked by the doctor, and the inherent token-spend of a real local plan), which other adoptability items must close.

### no-source-equivalence-gate [pipeline-engineering] · effort L
**Add a per-scenario assertion-coverage gate that reads the envelope's expectedAssertions (currently dead) and verifies each scenario block actually asserts each declared observable outcome**

- **Root cause:** Every gate is output-intrinsic. computeAggregateConfidence (scripts/evaluate.ts:446-462) scores plan-confidence + selector-quality + web-first + smell-removal + forbidden-absence plus the ungrounded cap — no term compares the migration's asserted behavior to the source. The one source-vs-output check, isAstDiffTrivial (evaluate.ts:362-376), only flags the OPPOSITE failure (overlap>0.8 = verbatim copy). The assertion gate is an absolute floor of 1 (evaluate.ts:304 ASSERTION_FLOOR=1; :323-324 passed=emitted>=floor), explicitly NOT compared to source (comment :298-303), so a source making 8 observable assertions whose migration emits 1 auto-ships; the baseline doc markets this floor-of-1 as 'no silent no-op' (measured-quality-baseline.md:41,72). The pipeline ALREADY produces the right data to do better: Stage 1 emits a 'User-perceivable assertion checklist' (prompts/analyze.md:76,307; config/migration-rules.md:671) which derive-envelope.ts:291-353 lifts into scenarios[].expectedAssertions (schema requires it and calls it 'Observable outcomes the generated test MUST check' — plan-envelope.schema.json:47,64-72). But that field has ZERO behavioral readers: it is declared in plan-code-coverage.ts:54-59 and plan-envelope-validate.ts:33 and never consulted — plan-code-coverage only checks that a '// plan:scenario=<id>' comment exists (collectScenarioPins :192, missing/orphan/dup :237-268). semantic-regression-check.ts is a misnomer (compares a fresh plan to a golden plan, never output-vs-source). Net: a migration that drops, weakens, inverts, mis-targets, OR invents assertions passes every gate. The tool's own outputs exhibit it: dashboard.page.ts invents toHaveCount(5)/name-pinned toContainText('Jane'), login.page.ts injects an un-sourced toBeHidden post-condition (baseline taxonomy items 1-2, :84-95), and the roulette proxy is structurally blind to assertions living in POM methods across test.step boundaries (evaluate.ts:188-195, own comment concedes the truncation).
- **Recommended solution:** Build a new deterministic post-Stage-2 validator, scripts/assertion-coverage.ts, that turns the already-emitted-but-dead envelope.scenarios[].expectedAssertions into an enforced gate, and wire it both as a hard CI check and as a confidence term. Mechanism: (1) parse each plan-checklist string into a small intent tuple {subject-keywords, matcher-class, expected-value/regex} using a fixed vocabulary the codebase already speaks — visible->toBeVisible/toBeHidden, contains 'X'/text->toContainText/toHaveText(/X/), URL ... /path->toHaveURL(/path/), count N->toHaveCount(N), enabled/disabled, checked. (2) Collect the EMITTED assertion tree per scenario: reuse collectEmittedSources() (evaluate.ts:633, already exported) to pull spec+POM+helper source, then use ts-morph (already a dep, used in plan-code-coverage.ts) to extract every `expect(...).<matcher>(<arg>)` call AND attribute it to a scenario by walking from the `// plan:scenario=<id>` pin to the next pin (pins already delimit test blocks — see expected-output.spec.ts). For POM-method assertions, attribute by following the page-object method the scenario's test block calls (the same fixture->file resolution collectEmittedSources already does). (3) For each scenario, require every expectedAssertion to be SATISFIED by at least one emitted assertion whose matcher-class and value-token match (fuzzy subject match: shared keyword OR shared value literal/regex). Emit GitHub annotations per unmatched checklist item: 'scenario 1.1 declares observable \"greeting contains Welcome back, Jane\" but no emitted assertion checks it (DROPPED/WEAKENED)'. (4) ALSO flag the inverse — emitted assertions on a scenario that match NO checklist item and carry no plan pin — as a softer WARN 'un-sourced assertion (possible invention/contamination)', directly targeting the dashboard.page.ts toHaveCount(5)/toBeHidden defects. (5) Default --warn for the first N migrations (calibration window) then flip to --strict once the false-positive rate is measured on the fixture corpus; gate exit 1 in strict. (6) Feed a coverageRate = matched/declared into computeAggregateConfidence as a new behavioral term (replace part of plan-confidence weight, e.g. 0.4->0.3 plan + 0.1 assertion-coverage) so a behaviorally-thin migration cannot reach 0.7 and is routed to verify even when locators look clean. This is intentionally a string-intent matcher (not full semantic equivalence) — it is deterministic, debuggable, calibratable on the existing fixture corpus, and catches the four named failure classes (drop/weaken/invert/mis-target) plus invention, without an LLM in the gate.
- **Alternatives considered:** (A) LLM-judge source-vs-output equivalence (Opus): highest recall but reintroduces non-determinism + cost the project explicitly bans for gates (CLAUDE.md debugging-discipline, ROADMAP 'NOT to do: prompt-tune'); rejected as the GATE but it already exists as the verify stage — this fix makes verify fire more often via the scorer term instead. (B) Raise ASSERTION_FLOOR to a source-ratio (e.g. emitted>=0.7*sourceAsserts): rejected by the codebase's own reasoning (evaluate.ts:298-303) — it fights the consolidation goal and false-positives on faithful migrations; matching the PLAN checklist (already deduped) avoids this. (C) Full AST semantic-equivalence / symbolic diff of behavior: far beyond effort budget and brittle on cross-framework source; the intent-string matcher captures ~the same failure classes at a fraction of the cost. (D) Only add the inverse 'un-sourced assertion' check (cheapest, targets invention/contamination): catches dashboard.page.ts/login.page.ts defects but misses the dropped/weakened/inverted classes which are the larger share — so it's folded in as the WARN half of the chosen solution rather than shipped alone. (E) Repurpose semantic-regression-check.ts: rejected — it's plan-vs-golden-plan by design; overloading it would muddy two distinct checks.
- **Concrete steps:**
  1. Add scripts/lib/assertion-intent.ts: a pure parser parseChecklistIntent(s:string)->{subjectKeywords:string[]; matcher:'visible'|'hidden'|'text'|'url'|'count'|'enabled'|'disabled'|'checked'|'value'|'unknown'; valueToken?:string|RegExp; negated:boolean} using the fixed vocabulary above (visible/hidden/contains/text/URL .../count N/enabled/disabled). Keep it small + unit-tested; 'unknown' matcher items become WARN-only so the parser's recall gaps never hard-block.
  1. Add scripts/lib/emitted-asserts.ts: extractEmittedAssertions(src:string, sf via ts-morph)->Array<{matcher:string; arg:string; line:number; file:string}> by finding CallExpressions whose chain is expect(...).<matcher>(...). Reuse the ts-morph Project pattern already in plan-code-coverage.ts:222-227.
  1. Add scripts/assertion-coverage.ts (mirror plan-code-coverage.ts CLI: --envelope, --output, --strict|--warn, --repo-root). Load envelope (reuse loadEnvelope shape), call collectEmittedSources (import the already-exported fn from evaluate.ts:633) to get spec+POM tree, attribute emitted asserts to scenarios via the `// plan:scenario=<id>` pin ranges (reuse collectScenarioPins logic from plan-code-coverage.ts:192-216 to get pin line numbers as scenario boundaries), then for each scenario.expectedAssertions[] require a matching emitted assert; emit ::error file=,line:: annotations for unmatched declared items and ::warning:: for un-sourced emitted asserts. Exit 1 in strict on any unmatched declared item.
  1. Create the calibration corpus tools/calibrate-pipeline/fixtures/assertion-coverage/{good-01-full-coverage, bad-01-dropped-assertion, bad-02-weakened-matcher, bad-03-inverted, bad-04-mistargeted-scenario, warn-01-invented-unsourced}/ each with code.ts + envelope.json (copy the existing plan-code-coverage fixture shape which already carries expectedAssertions like 'URL after submit matches /dashboard') + expected-output.txt. Register the validator in the calibrate runner so `npm run calibrate` exercises it (15 validators -> 16).
  1. Wire CI: in .github/workflows/migrate.yml add a step immediately after the plan-code-coverage step (~line 962-968) invoking `npx tsx scripts/assertion-coverage.ts --envelope ... --output ... --warn` (start in warn during the calibration window), and add the same to npm run validate:all / smoke so it runs locally zero-token.
  1. Wire the scorer: in scripts/evaluate.ts add coverageRate to the Report (computed via the same matcher in main(), reading the envelope path — thread --envelope into evaluate's args or derive it from --plan path), then in computeAggregateConfidence (:453-458) rebalance to plan*0.3 + selectorQuality*0.25 + webFirst*0.1 + smellRemoval*0.15 + forbiddenAbsence*0.1 + assertionCoverage*0.1; update renderReport's breakdown table (:489-496) and the unit tests in scripts/evaluate.test.ts. Keep the ungrounded cap unchanged.
  1. Add scripts/assertion-coverage.test.ts + scripts/lib/assertion-intent.test.ts covering each matcher class, the negated case (toBeHidden vs visible = INVERTED), POM-method-hosted asserts, and the un-sourced-WARN path. Run npm run smoke + npm run calibrate to confirm zero regressions on existing fixtures.
  1. Measure FP rate on the real n=6 bad-playwright batch in warm/warn mode, record it in docs/measured-quality-baseline.md, then flip the CI step and the strict default to --strict once FP is acceptable; update docs/measured-quality-baseline.md:41,72 to stop marketing the floor-of-1 as the parity signal and cite assertion-coverage instead.
- **Risk:** Primary risk is FALSE POSITIVES from a string-intent matcher: the pipeline is DESIGNED to consolidate brittle source probes into fewer web-first assertions (evaluate.ts:298-303), so a faithful migration can legitimately have FEWER emitted asserts than checklist items if one web-first assert subsumes two probes. Mitigation: match against the PLAN's expectedAssertions (Stage-1's deduped observable list), not the raw source's assert count — the checklist is already the consolidated intent, so 1-checklist-item->1-emitted-assert is the right granularity, not source-ratio. Second risk: matcher/subject fuzzy-matching mis-attributes (e.g. two scenarios both asserting visibility on different elements) — mitigate by requiring BOTH matcher-class AND a shared value/keyword token, and by scoping to the scenario's pin block. Third risk: parser recall — checklist strings are free-form prose; unparseable items map to matcher:'unknown' and become WARN-only (never hard-block) so the gate degrades safely. Fourth: the inverse 'un-sourced assertion' WARN will fire on legitimately-added good assertions (e.g. a defensible toHaveURL the plan's Risk-callouts recommended) — keep it WARN, never a blocker, and allow a `// plan:added=<why>` escape pin. Calibration on the fixture corpus + the n=6 real batch is mandatory before flipping to strict; ship in --warn first. Do NOT route this through an LLM judge — that reintroduces the non-determinism and cost the project's debugging-discipline explicitly forbids.
- **Indicator delta:** Closes the single largest correctness hole in the gate wall (H4, conceded in AUDIT-96-PREP.md:34-37): today a clean-but-behaviorally-wrong migration passes 100% of gates, and the measured 33%-acceptable-vs-67%-auto-ship gap (measured-quality-baseline.md:73) is substantially this defect class — 3 of the 4 named FIX-FIRST root causes (dropped/contaminated/invented assertions) are exactly what this gate catches deterministically. Estimate +10-14 points on pipeline-engineering toward 96%: it converts assertion-equivalence from 'unchecked' to 'checked + scored', and because coverageRate also feeds the scorer, it tightens the trustworthy-measured-quality indicator as a side effect (the scorer gains its first source-aware term). Conservative because it is an intent-string matcher, not full semantic equivalence: it catches drop/weaken/invert/mis-target/invent (the empirically dominant failures) but not subtle value-equivalence (e.g. /welcome/i vs /welcome back, jane/i both 'match'); closing that residual is a smaller follow-up. Net: the biggest single move available on pipeline-engineering, and the only one that directly attacks the 33%/67% trust gap at the gate layer rather than the generation layer.

### offline-gate-blind-on-cypress-selenium-and-noop-on-pw [multi-framework] · effort M
**Make the offline abstention gate column-aware and corpus-honest so it actually grounds Cypress/Selenium pins instead of silently dropping them**

- **Root cause:** The offline gate forms a LocatorPin only via pinFromTableRow (validate-plan-dom-grounding.ts:276-297), which requires locatorCellsOf to find >=2 "locator-shaped" cells in a row, where locator-shaped is defined by isLocatorExpression (:342-347) as ONLY page.getBy*/page.locator. It then takes the New cell as locatorCells[1] — i.e. it assumes BOTH the Original and New columns are Playwright locators. That assumption holds only for subtractive bad-PW plans. Cypress/Selenium plans (the entire multi-framework corpus) put NATIVE syntax in the Original column (cy.get, cy.contains, @FindBy, By.cssSelector, driver.find_element), so each row yields exactly ONE locator-shaped cell (the New column), hits locatorCells.length < 2 at :279, and returns null. Every pin is silently dropped; all 5 cross-framework plans plus all bad-PW plans exit 0. I reproduced this: checkout-flow.cy.js, EmployeesTest.java, and using_selenium_tests.py all print "offline abstention gate passed" and exit 0, even though using_selenium has a HIGH row and the Selenium-Java plan has HIGH rows with names. Two compounding defects: (b) validateOfflineAbstention (:456) skips low/medium rows, which is exactly where checkout-flow (0 HIGH) and nth-selectors (0 HIGH) live, so entirely-uncertain plans are never checked; (c) for bad-PW HIGH rows the Original cell joins the corpus (:450) and the names are copied verbatim from source, so nameDerivableFromSource is trivially true — it proves the LLM COPIED the name, not that the element exists. Net: the only grounding that runs by default (no SUT) provides zero coverage on 100% of the measured corpus, while CI prints a green pass that predicts nothing.
- **Recommended solution:** Three-part fix, all inside validate-plan-dom-grounding.ts plus its test and the analyze.md contract. (1) Decouple pin formation from the Original column being a PW locator: replace the position-based pinFromTableRow with a HEADER-AWARE parser that locates the "New" and "Confidence" columns by their header labels (the canonical order from analyze.md:101 is | Original | New | Confidence | Notes |, with EmployeesTest using | Original (file:line) | New | Confidence | Notes |). Form a pin whenever the New cell contains a PW locator, regardless of what the Original cell holds. This alone makes the gate fire on all cross-framework HIGH/derivable-failing rows. (2) Broaden the corpus to include the NATIVE Original-column text, not just PW-shaped Original cells: in validateOfflineAbstention (:450), build the corpus from the raw Original cell strings of ALL rows (extract the backticked literal even when it is cy.get('.summary-total') or By.id("search-employees")), so a New name like "Search" is checked against the native source vocabulary. Without this, every cross-framework HIGH name would now FAIL (over-trigger) because the native Original text was previously excluded. This keeps the gate honest: a name present in the native source selector/string is a translation, a name absent is a confident invention. (3) Tighten the contract so the gate is not a copy-detector: in analyze.md, mandate that a HIGH-confidence role/name pin whose accessible name is NOT a verbatim substring of the source MUST be downgraded to MED or carry an inline // dom-snapshot annotation; document that HIGH+name now requires the name to appear in source OR an annotation. Do NOT expand the gate to fail low/medium (defect b) by hard-blocking — instead emit a non-blocking ::warning for low/medium role/name pins whose name is NOT derivable AND has no defense pin, surfacing the uncertain-name plans (checkout-flow, nth-selectors) in CI without false-failing legitimate honest guesses. Separately, fix the latent cross-language probe no-op in migrate.yml:1162 by deriving BASE from the kebab-case Stage-2 stem (reuse the rename logic in plan-envelope-validate.ts:143-147) rather than the source basename, so find matches employees.spec.ts not EmployeesTest.java; this is low-risk because dom_probe_status is currently never read downstream (I grepped — zero consumers), but it removes a false 'pass'.
- **Concrete steps:**
  1. In /Users/kapusansky/DEV/PWmodernizer/scripts/validate-plan-dom-grounding.ts, capture the table HEADER row in extractLocatorPins (:228) before tableSeparatorSeen flips: split the header into labels, find the index of the column whose label starts with 'new' and the one starting with 'confidence'. Store these on a per-table basis.
  1. Rewrite pinFromTableRow (:276-297) to accept the resolved newIdx/confidenceIdx: extract the New locator via extractLocatorFromCell(cells[newIdx]); if null, return null. Read confidence from cells[confidenceIdx] (fall back to confidenceFromCells scan if header absent). Set original = extractOriginalLiteral(cells[originalIdx]) where extractOriginalLiteral pulls the backticked literal even for native syntax (cy.get(...), @FindBy(...), By.x(...), driver.find_element(...)). REMOVE the locatorCells.length < 2 guard — the New column alone forms the pin.
  1. Add a helper extractOriginalLiteral(cell) that returns the first backtick-wrapped span of the cell verbatim (no isLocatorExpression filter), so native Original selectors feed the corpus.
  1. In validateOfflineAbstention (:448-463), change originalCorpus (:450) to join the new pin.original native literals; keep sourceText. This is the corpus that nameDerivableFromSource checks against — now it includes cy.get('.summary-total'), By.id('search-employees'), etc.
  1. Keep the HARD failure for high/null pins (:456 unchanged for high), but ADD a soft path: for low/medium role/name pins whose name is NOT derivable AND that have no matching defense pin, collect into a separate warnings[] list; emit them as ::warning in runOfflineAbstention (:531) without changing the exit code. This surfaces checkout-flow/nth-selectors uncertainty in CI without false-failing.
  1. Update /Users/kapusansky/DEV/PWmodernizer/scripts/validate-plan-dom-grounding.test.ts: add cases for (a) a Cypress row | `cy.get('.summary-total')` | `page.getByText('Order confirmed')` | high | with 'Order confirmed' ABSENT from source → exit 1, and present → exit 0; (b) a Selenium-Java | `@FindBy(...)` | `page.getByRole('button',{name:'Invite'})` | high | row → fires; (c) a low/medium ungrounded name emits a warning but exit 0.
  1. Run the gate against the real corpus to confirm it now fires correctly: npx tsx scripts/validate-plan-dom-grounding.ts --plan outputs/plans/EmployeesTest.java.md --source inputs/selenium-java/EmployeesTest.java (expect a HIGH name absent from source to exit 1, or pass only if derivable). Re-run for checkout-flow.cy.js, using_selenium_tests.py, wishlist.cy.js, test_employees.py.
  1. In /Users/kapusansky/DEV/PWmodernizer/prompts/analyze.md §4 (around :113-126), add a mandatory rule: a HIGH-confidence getByRole/getByLabel name MUST be a verbatim token of the source selector/string OR carry an inline // dom-snapshot annotation; otherwise downgrade to MED with a defense pin. Mirror the rule into the Hallucination-defense section.
  1. In /Users/kapusansky/DEV/PWmodernizer/.github/workflows/migrate.yml dom-ground loop (:1162), derive BASE from the kebab-case spec stem (import/reuse the rename in scripts/plan-envelope-validate.ts:143-147) so `find outputs/tests/${BASE}` matches the actual Stage-2 output; this removes the silent zero-iteration false 'pass' on cross-language inputs.
  1. Run npm run smoke and the calibration corpus (npm run calibrate) to confirm no bad-PW regressions (the fix must not start failing the previously-green bad-PW plans where names are legitimately copied — those remain derivable and pass).
- **Risk:** Over-trigger / false positives: step 2 makes the gate fire on every cross-framework HIGH role/name row, so if the corpus-broadening (step 4) is incomplete, legitimate honest translations (e.g. getByRole('button',{name:/add/i}) derived from @FindBy text 'Add') would FALSE-FAIL and block the multi-framework plans the project wants to promote. Mitigation: the corpus MUST include both the native Original literal AND the source file text, and nameDerivableFromSource already tolerates squashed/spaced token matching — verify on all 5 real plans before merge. Calibration risk: the squash/token heuristic was tuned for English PW names; native selectors like By.id('search-employees') contain the tokens, but icon-only buttons (remove, send) may have names absent from source and would now hard-fail HIGH rows — that is the CORRECT behaviour (those are exactly the unverified inventions), but it will newly fail any plan that marked them HIGH, so expect some plans to need a HIGH→MED downgrade (a real quality improvement, not a regression). The soft-warning path (step 5) must NOT flip exit code or it reintroduces blocking on every low/medium guess. The migrate.yml BASE fix (step 9) is inert today (no consumer of dom_probe_status) so it carries near-zero behavioural risk but must be kept in sync with the kebab rename logic to avoid drift. Header-parsing risk: if a future plan omits the Confidence header or reorders columns, fall back to the existing confidenceFromCells scan rather than crashing.
- **Indicator delta:** Multi-framework is the bottleneck here: today the ONLY default-active grounding provides 0% coverage on Cypress/Selenium (the gate exits 0 on all 5 cross-framework plans regardless of content), so adopters get a false-green that actively undermines trust in promoting Cypress/Selenium beyond example status. Fixing pin formation + corpus honesty makes the gate real on the cross-framework corpus for the first time, converting a predicts-nothing pass into an actual confident-hallucination check that catches invented accessible names in native-syntax plans. I estimate this moves multi-framework roughly +8 to +12 points toward 96% (e.g. from a ~70 baseline into the low 80s): it is necessary but not sufficient — it does not by itself measure the 70% acceptable-rate bar at scale (H5), and it does not verify semantic equivalence to the source (H4). It removes the single biggest FALSE signal blocking honest promotion, and the analyze.md contract tightening (step 8) compounds it by reducing copied-name HIGH pins at the source. Secondary credit to pipeline-engineering (the latent cross-language probe no-op is removed) and trustworthy-measured-quality (CI green now means something on the real corpus).

