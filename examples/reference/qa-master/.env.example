# Environment selection: true = production, anything else = www-master staging
IS_PRODUCTION=false

# Optional override for the staging base URL
# URL_BASE=https://www-master.staging.customink.com

# No shared test account is needed: each test creates its own user via the signup API
# (see helper/api/accounts.api.ts). Password used for created accounts is a constant there.
