import * as path from 'path';

/** Absolute path to a small test image used by upload tests. */
export const TEST_UPLOAD_FILE = path.join(__dirname, 'files', 'test-upload.jpg');
export const TEST_UPLOAD_FILE_NAME = 'test-upload.jpg';
