# Exit 1. The bad selenium-python explicit-wait fixture seeds 4 anti-patterns: import from
# @playwright/test, page.goto in spec, raw `.form-error` CSS-class locator (warn — the migrator
# carried the selenium By.CSS_SELECTOR('.form-error') verbatim instead of lifting to
# getByRole('alert')), and a page.waitForTimeout(5000) that was mistranslated from the selenium
# WebDriverWait(driver, 5) explicit wait. Block rules to anchor on: import-source,
# page-goto-in-spec, no-hard-waits. Summary: 5 total / 3 block / 2 warn (the second warn is
# click-without-assertion on the signIn() page method — the spec's missing post-click landing
# assertion + the bad page object's loose signIn() both come from the same migration smell).
qa-master/architecture/import-source
qa-master/architecture/page-goto-in-spec
qa-master/runtime/no-hard-waits
validate-qa-master-conformance: 5 violation(s) (3 block / 2 warn)
