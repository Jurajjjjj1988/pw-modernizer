# Exit 0. The good selenium-python explicit-wait fixture replaces every WebDriverWait + page_source
# substring probe with web-first toHaveURL / toBeVisible auto-waiting assertions, lifts the XPath
# dashboard header to getByRole('heading', { name: 'Dashboard' }), and asserts the inline sign-in
# error via getByRole('alert') (no `.form-error` CSS class survives). The validator MUST accept it
# as clean.
validate-pwm-blueprint-conformance:
checked — clean.
